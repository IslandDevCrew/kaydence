// Presentation only. The Rust backend owns all logic (root AGENTS §9).
export function App(): JSX.Element {
  return (
    <main className="shell">
      <h1>
        Kaydence <span className="dot">●</span>
      </h1>
      <p>P0 scaffold — speak anywhere, ship clean text.</p>
    </main>
  );
}
