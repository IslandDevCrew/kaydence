---
description: Summarize Kaydence build state and the next work units
---

Print a concise status recap, no code changes:
1. Current ROADMAP phase and its exit criteria (with which are met).
2. Last commit (hash + message) and the work unit it closed.
3. The evidence ledger for that unit (claim → artifact → result).
4. Any open human gate or blocker (and which command resolves it).
5. The next 3 work units in ROADMAP order, each with its PRD ID.
Keep it scannable. End by asking for `go`, `clarify`, or `rewind`.
