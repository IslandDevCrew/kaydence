Kaydence first-run model picker recommendation evidence
Date: 2026-07-08

Intent
- Move the first-run ASR picker from hardcoded display copy to backend-owned
  registry state.
- Expose the recommended and selected ASR model ids through AppSnapshot.
- Keep downloads, artifact URLs, and inference adapters out of this slice.

Changed files
- apps/desktop/src-tauri/src/settings/mod.rs
  - Added FirstRunAsrCandidate plus recommended_asr_model_id and
    selected_asr_model_id on FirstRunStatus.
- apps/desktop/src-tauri/src/lib.rs
  - First-run model readiness now also derives recommended ASR candidates from
    the registry.
  - Recommendation rule: prefer an OS-specific registry default_for match; else
    choose a CPU-safe/min_hw=any candidate; else the first recommended ASR.
  - Candidate rows carry state/detail from the same artifact verification used
    for required model readiness.
  - Added tests for candidate serialization, CPU-safe fallback selection, and
    OS-default override behavior.
- apps/desktop/src/App.tsx
  - Uses selected_asr candidate for the cockpit engine label when present.
  - Renders a compact Local ASR picker from backend asr_candidates.
- apps/desktop/src/styles.css
  - Added dense model-picker styles consistent with the existing first-run
    setup panel.
- ops/mission/state.json and docs/CODEX_SUPER_GOAL_PROMPT.md
  - Updated P1-P0-2 status and next-action guidance.

Verification
- PASS: cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml --all -- --check
- PASS: cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml runtime_snapshot --lib
  - 5 runtime snapshot tests passed.
- PASS: cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml settings:: --lib
  - 8 settings tests passed.
- PASS: cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml os_default_asr_recommendation --lib
  - 1 recommendation test passed.
- PASS: cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml
  - 136 library tests passed.
  - 2 crash recovery tests passed.
- PASS: cargo clippy --manifest-path apps/desktop/src-tauri/Cargo.toml --all-targets -- -D warnings
- PASS: npm --prefix apps/desktop run check
- PASS: npm --prefix apps/desktop run build
- PASS: bash scripts/audit-network.sh --check
  - 0 network call sites found.
- PASS: bash scripts/check-adr-status.sh
  - ADR-0009 waiver only.
- PASS: ./script/build_and_run.sh build
- PASS: ./script/build_and_run.sh --verify

Honest limits
- This does not download model artifacts.
- This does not replace registry TODO hashes/sources with reviewed published
  artifacts.
- This does not persist an edited model selection yet.
- This does not implement Parakeet/Whisper inference adapters or WER/latency
  measurement.

Post-verify hygiene
- Generated Vite dist files were restored/removed after build verification.
- Process check after verification:
  ps axww -o pid=,command= | rg "[c]argo tauri dev|[v]ite --host|target/debug/[k]aydence|/[K]aydence.app" || true
  - Expected: no output.
