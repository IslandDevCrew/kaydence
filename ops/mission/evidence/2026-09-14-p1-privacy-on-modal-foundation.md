# P1-G4 Privacy readability on the keyboard/scroll foundation
- Parent: frozen modal packet `2835f544aa560703a839c09c6a5fb84b7b4633ec`; actual frontend at port 1434. Local verification only; parent reconciliation, independent Judge, standing gates and CI remain required before publication.
- Rebased only Privacy body/History-ring/evidence and its prior refresh; body CSS remains byte-identical (`d0e8976c…`). Historical `97a55f7` evidence/checker and the prior `c4b9a79` raw proof are preserved.
- Product scope is only `PrivacyView.css`: restore token-based 11px floor, complete policy text, natural vertical reading, narrow reflow and the History-button scroll margin. No copy, JS, backend, IPC, dependencies, state, identity, palette or navigation changes.
- Composition keeps History 8px margin and inherited marker-aware `--focus-ring`. Modal focus/actual-opener behavior, inner overscroll `none` and outer `contain` are inherited unchanged from the corrected parent.
- Same primary checker: historical predecessor fails all 24 cases; refreshed actual bundle passes 24/24 across three OS reference presentations, both themes and 450/500/501/900 widths. This is CSS viewport evidence, not physical OS scaling.
- Actual full-reading/keyboard checks pass Chromium 6/6 and WebKit Option+Tab 6/6 at 450x300, 501x300 and 900x600, both themes: complete text, Close after reading, opener return, reverse traversal and History outline; zero overrides. Separate stronger modal checks pass 6/6 per engine, requiring settled bottom and unchanged background, not mere movement.
- The Source/allowlist-audit linebox advisory remains raw; captures show separate readable glyphs. At 900px the Context panel has excess lower whitespace and Audit falls below the fold: density/family fidelity remains open, not polished or whole-WCAG approved.
- Hidden-section injection fails 24/24; six additional full-reader mutations (hidden, ellipsis, 10px in both engines) all fail for the intended reason. Fresh frontend typecheck/lint and diff check pass; no acceptance assertion is weakened.
- Painted focus gate passes 96 scopes / 768 states, qualifying-side minimum 3.7451306265; no baseline geometry comparison, all-pixels or AAA-area claim.
- Eight refreshed screenshots, current raw/source bindings and temporary plain-runner hashes are manifest-bound; `raw-and-bindings.json.gz` also retains the prior raw packet and capture bytes. No source scripts are compressed.
- Reproduce primary with bundled `PLAYWRIGHT_MODULE`, `APP_URL=http://127.0.0.1:1434`, fresh `CAPTURE_DIR`, and `node ops/mission/evidence/2026-09-07-p1-privacy-readability-check.cjs`. Add `INJECT_HIDDEN=1` for the failing negative.
- Full-reader: `/tmp/kaydence-modal-b-integrated/full-privacy.cjs`, `ENGINE=chromium|webkit`, same URL and fresh capture directory, **no** `PRIVACY_STYLE_REF`. Exact paths/hashes and raw observations are archived.
- WebKit Option+Tab is explicitly labeled; browser/system settings were not changed. Native WebView/VM/hardware acceptance and broader P1-G4 remain open; this packet does not claim completion of the other screens.
