# Setup main-board readability — local integration, 2026-09-14

This local packet replays only the original Setup unit from 9e47df3 onto Evidence parent e76b762. FirstRunView.css is the sole product diff; the stronger parent's FirstRunEvidence.css, keyboard markers, derived focus colors, and modal containment remain intact. All original evidence is preserved.
The additional cures give narrow footer proof a full-width row above the existing actions without DOM-order changes, and apply the existing 8px native-focus margin to board buttons, selects, and inputs (the heading's established 4px override remains). No content, commands, palette, identity, or 900px at-rest geometry changed.
Actual served-source checks pass: 32 natural default/long-blocked cases across Chromium151/WebKit26.5, both themes, 900×600 and 450/501/600×300; 1,368 whole focus cues; 24 legacy structural cases; 96 focus-color scopes/780 states; strict frontend and direct Vite build. All 16 deliberate negative probes fail closed. Four desktop comparisons preserve 256-element geometry and settled screenshot identity; Chromium's first-frame native-control repaint also occurs on same-CSS repeat and is retained separately.
Preserved RED includes the 59.8px-wide long-proof column and the naturally focused Toggle ring above the scroll boundary. The original 1px Range overhang is not claimed as painted-glyph clipping. WebKit can hit its scroll ancestor over visibly painted edge text; the reader retains all character/clip bounds, rejects unrelated overlays, and separately proves text-only occlusion fails after successful wheel input.
Reproduce from this worktree using PLAYWRIGHT_MODULE=/Users/IDC2.5/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright and APP_URL=http://127.0.0.1:1446 with /tmp/kaydence-setup-main-board/natural-board.cjs. The raw bundle binds all temporary plain runner paths/hashes, exact product/parent sources, logs, JSON, and archived PNG hashes; no source scripts are compressed into evidence. Negative and parity runner paths are bound alongside it.
This is browser-only readability/reachability evidence, not native WebView, physical-device proof, full screen-family fidelity, or P1-G4 closure. WebKit uses explicitly labeled Option+Tab without changing system settings. Independent review and final actual-parent integration remain pending; long blocked data is a source-shaped fixture, never readiness proof.
