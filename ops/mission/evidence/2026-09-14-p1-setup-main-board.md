# Setup main-board readability — local integration, 2026-09-14

This local packet replays only the original Setup unit from 9e47df3 onto Evidence parent e76b762, then combines it unchanged with Privacy + Evidence parent 88ce218. FirstRunView.css is the sole product diff; the stronger parent's FirstRunEvidence.css, keyboard markers, derived focus colors, and modal containment remain intact. All original evidence is preserved; initial.json.gz and integrated.json.gz remain immutable, while guarded.json.gz binds the supplemental visibility-oracle cure without product changes.
The additional cures give narrow footer proof a full-width row above the existing actions without DOM-order changes, and apply the existing 8px native-focus margin to board buttons, selects, and inputs (the heading's established 4px override remains). No content, commands, palette, identity, or 900px at-rest geometry changed.
Current v2 served-source checks pass: 32 natural default/long-blocked cases across Chromium 151/WebKit 26.5, both themes, 900×600 and 450/501/600×300; 1,368 whole focus cues; all 16 retained negative probes. The unchanged product also passed 96 focus-color scopes/780 states, strict frontend/build, and 24 legacy structural cases. Four earlier desktop comparisons preserve 256-element geometry and settled screenshot identity; Chromium's first-frame native-control repaint also occurs on same-CSS repeat and remains separately recorded.
Preserved RED includes the 59.8px proof column, clipped Toggle ring, and independent HOLD showing v1 accepting 140 characters under opaque ancestor paint. The 1px Range overhang is not claimed as glyph clipping. V2 retains bounds, fonts, wheel and unrelated-overlay checks but rejects unsupported ::before/::after paint when accepting a scroll-ancestor hit. Independent 450/501 probes reproduce four WebKit successful-wheel old false-greens becoming unread-red; Chromium blocks input instead. Own replay attempts with failed old input remain explicitly inconclusive, not PASS. This is not general arbitrary-paint visibility proof.
Reproduce from this worktree using PLAYWRIGHT_MODULE=/Users/IDC2.5/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright and APP_URL=http://127.0.0.1:1446 with /tmp/kaydence-setup-main-board-oracle-v2/natural-board.cjs. This supplemental plain runner is path/hash-bound, not portable; the checked-in structural checker remains. Original f370 runner/results stay historical. Raw bundles bind temporary runner paths/hashes, source digests, logs, JSON, PNG hashes and independent negative proof; no source scripts are compressed into evidence.
This is browser-only readability/reachability evidence, not native WebView, physical-device proof, full screen-family fidelity, or P1-G4 closure. WebKit uses explicitly labeled Option+Tab without changing system settings. Independent review and final actual-parent integration remain pending; long blocked data is a source-shaped fixture, never readiness proof.
