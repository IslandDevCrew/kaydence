# Shared modal custody integration — BLOCKED, not keyboard acceptance

Rebased only shared-scroll commits onto opener `10472b9`; product freeze `c9af9fe`, Modal SHA256 `9a36066bc4c8323ef9498bfbf8c2bc845dd338ef4d467e55af67e4ac491c2930`. Explicit actual opener/fallback priority, disabled guards, nearest actual/centered fallback reveal, first-control focus, containment and margins compose; document.activeElement is never opener authority. No backend/palette/geometry/state/publication changes.
Fresh actual Vite1438: Chromium/WebKit pointer164 + pending-IPC24 PASS; Chromium wheel12 + faithful Privacy-body12 PASS; Chromium full-read Privacy6/Setup6 PASS; frontend typecheck/eslint PASS. Bodies are explicit frozen CSS fixtures (Privacy9e779ec, Setupce5db64), not merged/native fidelity.
WebKit26.5 Option+Tab Privacy full-read6 PASS, but Setup full-read6 FAIL:36 missing outlines after non-control text click then handled reverse/forward Tab. Active controls stay in-dialog and content remains readable, but :focus-visible=false/outline:none is an actual regression, not a color pass.
Differential: identical all-Option Setup runner against opener1440 passes6/6. No-overlay minimal trace confirms native reverse traversal on1440 yields visible proof-button focus, while integrated unmanaged interception on1438 preventDefaults and focuses Recheck models/Close without rings; later native traversal restores rings. The unit must not merge with this introduced defect.
Authorized smallest-removal experiment (Modal7473f0ee) restores WebKit6/6 but breaks Chromium12/12 base +6/6 Privacy at real content-click→Shift+Tab, which escapes the open dialog to BODY. Experiment reverted exactly to9a36066; no guard/assertion weakened. A separately scoped visible keyboard-intent cure is required; no new mechanism is silently added here.
Fresh ordinary-WebKit-Tab app sequences still fail to reach buttons. Parent's unstyled HTML control probe reproduces skipped buttons/links while inputs/selects remain reachable; this is environment-baseline equivalence, not proof of an app-specific Tab defect. Original BODY-only traces are historical. Native full-keyboard settings proof remains pending; no settings were changed, and Option+Tab is not default-Tab acceptance.

`2026-09-14-p1-modal-custody-integration/raw-and-bindings.json.gz` contains 36 verbatim raw JSON/log records plus exact source/runner SHA256 bindings (no compressed source scripts), including all RED/GREEN/differential/failed-removal evidence. Two PNGs compare the same pointer-text→Option+Shift+Tab action before/after integration. Old source-bound manifests remain historical, not current-tree validation.
Reproduce the three checked-in modal checkers with bundled PLAYWRIGHT_MODULE, APP_URL=http://127.0.0.1:1438 and fresh CAPTURE_DIR. Raw metadata names the plain full-reader/differential runners under `/tmp/kaydence-modal-custody-integrated/`; never overwrite their frozen outputs. No QA-forced focus/scroll was used; CSS fixtures and alternate keyboard mode are explicit.
HALT: introduced WebKit ring regression blocks this integration. Independent Judge, fresh full standing gates, exact-head CI/CLEAN/merge and native/P1-G4 closure remain main-owned and pending; no prior Chromium PASS waives the red cross-engine gate.
