# Kaydence Mission Journal

## 2026-07-09T18:15Z — Codex P1 slice (macOS Input Monitoring proof refresh)
- Added a no-prompt macOS Input Monitoring preflight through `IOHIDCheckAccess(kIOHIDRequestTypeListenEvent)` inside the macOS injector platform boundary.
- `refresh_first_run_runtime_proofs` can now mark the first-run Input Monitoring row Ready when macOS reports listen-event access is granted. That proof also satisfies `input_permission_ready`; hotkey registration, microphone proof, model readiness, and first dictation remain separate gates.
- Updated the first-run proof wording so the ready boundary accepts either the IOHID listen-event grant or a real global-hotkey runtime event, while Microphone still requires non-empty persisted capture audio.
- Evidence saved at `ops/mission/evidence/2026-07-09-macos-input-monitoring-proof-refresh.txt`. Local gates passed: fmt check, focused permission tests (12), full Rust lib suite (225 tests), clippy with `-D warnings`, and frontend production build.
- Remote boundary unchanged: local work remains unpushed because GitHub Actions hosted runners are still blocked before checkout by billing/spending-limit state. Latest green remote baseline remains `ca60244` / run `29031546518`; latest checked blocked run remains `f61ab49` / `29033270673`.

## 2026-07-09T18:08Z — Codex P1 slice (macOS Accessibility proof refresh)
- Added a macOS Accessibility preflight through `AXIsProcessTrusted` inside the macOS injector platform boundary.
- `refresh_first_run_runtime_proofs` now applies platform permission proofs after draining active hotkey/audio proofs; when macOS trusts this Kaydence process for Accessibility, the first-run Accessibility row becomes Ready with runtime proof detail.
- Kept the readiness boundary honest: Microphone still needs non-empty persisted capture audio, Input Monitoring still needed live input proof at this checkpoint, and native insertion/secure-field refusal proof remained separate.
- Evidence saved at `ops/mission/evidence/2026-07-09-macos-accessibility-proof-refresh.txt`. Local gates passed: fmt check, focused permission tests, full Rust lib suite (224 tests), clippy with `-D warnings`, frontend production build, and diff whitespace check.
- Remote boundary unchanged: local work remains unpushed because GitHub Actions hosted runners are still blocked before checkout by billing/spending-limit state. Latest green remote baseline remains `ca60244` / run `29031546518`; latest checked blocked run remains `f61ab49` / `29033270673`.

## 2026-07-09T18:02Z — Codex P1 slice (cleanup default dial persistence)
- Wired the visible Cleanup dial to real backend behavior: `set_cleanup_dial` parses Raw/Light/Full, persists `cleanup_default_dial` in app-data `settings.json`, updates `AppSnapshot`, and refuses runtime mutation during active capture.
- The hotkey runtime now carries a persisted cleanup default for non-user-edited profile targets, while user-edited per-app profiles still override it and the Shift+RightAlt/Option Raw override remains a one-capture override.
- Evidence saved at `ops/mission/evidence/2026-07-09-cleanup-default-dial-persistence.txt`. Local gates passed: fmt check, full Rust lib suite (222 tests), clippy with `-D warnings`, frontend production build, and diff whitespace check.
- Remote boundary unchanged: local work remains unpushed because GitHub Actions hosted runners are still blocked before checkout by billing/spending-limit state. Latest green remote baseline remains `ca60244` / run `29031546518`; latest checked blocked run remains `f61ab49` / `29033270673`.

## 2026-07-09T17:52Z — Codex P1 slice (secondary cleanup override hotkey)
- Added runtime support for the secondary per-invocation cleanup override chord: `Shift+RightAlt` / `Shift+RightOption` can register alongside the selected primary global shortcut as a Raw-only capture path.
- The hotkey runtime now tracks which shortcut role started a capture, ignores mismatched release edges, applies `CleanupDial::Raw` only to the override capture, and returns the next primary capture to the detected profile cleanup dial.
- Evidence saved at `ops/mission/evidence/2026-07-09-secondary-cleanup-override.txt`. Local gates passed: fmt, focused override/shortcut tests, full Rust lib suite (215 tests), clippy with `-D warnings`, and diff whitespace check.
- Remote boundary unchanged: local work remains unpushed because GitHub Actions hosted runners are still blocked before checkout by billing/spending-limit state. Latest green remote baseline remains `ca60244` / run `29031546518`; latest checked blocked run remains `f61ab49` / `29033270673`.

## 2026-07-09T17:42Z — Codex continuation slice (current handoff docs)
- Refreshed `docs/CODEX_SUPER_GOAL_PROMPT.md` from current repo truth: private GitHub access restored, `origin/main` at `f61ab49`, local `main` ahead by 8 commits, latest green 3-OS baseline `ca60244` / run `29031546518`, and hosted Actions currently billing/spending-limit blocked before checkout.
- Added `docs/WINDOWS_CODEX_HANDOFF_2026-07-09.md` and marked the July 8 Windows handoff as superseded. The new handoff reflects that Windows injection is merged/proven and now focuses the Windows session on rerunning proof after local follow-up commits, Windows first-run permission proof, Screen Family 10, hotkey/first-dictation proof, and reference-machine timing.
- Marked the July 8 markdown SOTU as historical evidence only; the generated `ops/mission/state-of-the-union.html` plus `ops/mission/state.json` remain the current state-of-truth pair.
- Evidence saved at `ops/mission/evidence/2026-07-09-current-continuation-handoff.txt`. Local validation passed: handoff content search checks, state JSON parse, SOTU render, and diff whitespace check.

## 2026-07-09T17:38Z — Codex P1 slice (permission settings opener)
- Added backend-owned settings targets to first-run permission guidance and a desktop command that can open the stable OS settings target when one exists.
- Screen Family 10 now renders a separate Open Settings action inside the permission proof card; requirements without a stable settings panel remain manual-only. Opening settings never marks a permission row ready.
- Evidence saved at `ops/mission/evidence/2026-07-09-permission-settings-opener.txt` plus in-app browser screenshot `ops/mission/evidence/2026-07-09-permission-settings-opener-preview.png`. Local gates passed: frontend check, fmt check, focused first-run permission tests, full Rust lib tests, Browser smoke, production build, desktop verify, and clippy.
- Remote boundary unchanged: local work remains unpushed because GitHub Actions hosted runners are still blocked before checkout by billing/spending-limit state. Latest green remote baseline remains `ca60244` / run `29031546518`; latest checked blocked run remains `f61ab49` / `29033270673`.

## 2026-07-09T17:31Z — Codex P1 slice (first-run action label)
- Aligned the bottom Screen Family 10 setup primary action with backend-owned `next_step.action_label` instead of showing the generic `Resolve Setup` label while first-run readiness is still false.
- The next-step card action and bottom primary action now say the same thing in the current model-metadata state: `Review models`.
- Evidence saved at `ops/mission/evidence/2026-07-09-first-run-action-label.txt` plus in-app browser screenshot `ops/mission/evidence/2026-07-09-first-run-action-label-preview.png`. Local gates passed: frontend check, fmt check, Browser smoke, production build, desktop verify, and clippy.
- Remote boundary unchanged: local work remains unpushed because GitHub Actions hosted runners are still blocked before checkout by billing/spending-limit state. Latest green remote baseline remains `ca60244` / run `29031546518`; latest checked blocked run remains `f61ab49` / `29033270673`.

## 2026-07-09T17:24Z — Codex P1 slice (first-run UI permission contract)
- Aligned the React first-run checklist with the stricter backend readiness contract: the visible `Grant OS permissions` row now requires every rendered permission requirement row to be Ready, not just the coarse microphone/input booleans.
- The setup action readiness flag now uses the same helper, preventing the UI from looking ready while Windows UIA/SendInput, Linux AT-SPI/uinput, or native secure-field proof rows still need hardware/review evidence.
- Evidence saved at `ops/mission/evidence/2026-07-09-first-run-ui-permission-contract.txt` plus in-app browser screenshot `ops/mission/evidence/2026-07-09-first-run-ui-permission-contract-preview.png`. Local gates passed: frontend check, focused backend readiness test, in-app Browser smoke, production build, desktop verify, and clippy.
- Remote boundary unchanged: local work remains unpushed because GitHub Actions hosted runners are still blocked before checkout by billing/spending-limit state. Latest green remote baseline remains `ca60244` / run `29031546518`; latest checked blocked run remains `f61ab49` / `29033270673`.

## 2026-07-09T17:19Z — Codex P1 slice (first-run ready permission contract)
- Tightened `FirstRunStatus.ready_to_dictate()` so it cannot report ready from coarse model/mic/input/hotkey booleans alone.
- Added an explicit `permission_requirements_ready()` invariant: top-level readiness now requires every Screen Family 10 permission row to be Ready, matching the checklist and exported proof JSON a user/build agent sees.
- This prevents Windows UIA/SendInput or Linux AT-SPI/uinput/native secure-field rows from being bypassed by a broad runtime proof flag.
- Evidence saved at `ops/mission/evidence/2026-07-09-first-run-ready-permission-contract.txt`. Local gates passed: fmt, focused readiness/proof tests, clippy, full Rust suite (212 lib + crash/event/short suites), frontend check, privacy posture, ADR status, bench PASS/PARTIAL, production build, and desktop verify.
- Remote boundary unchanged: local work remains unpushed because GitHub Actions hosted runners are still blocked before checkout by billing/spending-limit state. Latest green remote baseline remains `ca60244` / run `29031546518`; latest checked blocked run remains `f61ab49` / `29033270673`.

## 2026-07-09T17:11Z — Codex P1 slice (runtime permission proof refresh)
- Added a backend-owned runtime proof refresh for first-run setup: real active global-shortcut events mark input proof, non-empty finalized capture audio marks microphone proof, and first-dictation completion syncs those runtime-proven rows.
- Screen Family 10 now has a compact Refresh action beside Free selected. In the desktop runtime it drains pending hotkey proofs through `refresh_first_run_runtime_proofs`; in Vite preview it shows the desktop-runtime boundary instead of pretending native IPC ran.
- Kept the boundary honest: zero-sample WAL-only captures do not mark microphone ready, and Windows UIA/SendInput plus Linux AT-SPI/uinput proof rows remain manual/review-gated until their platform evidence exists.
- Evidence saved at `ops/mission/evidence/2026-07-09-runtime-permission-proof-refresh.txt` plus preview screenshot `ops/mission/evidence/2026-07-09-runtime-permission-proof-refresh-preview.png`. Local gates passed: fmt, lib tests (211), frontend check, clippy, full Rust suite, privacy posture, ADR status, bench PASS/PARTIAL, production build, desktop verify, and Chrome smoke.
- Remote boundary unchanged: local work remains unpushed because GitHub Actions hosted runners are still blocked before checkout by billing/spending-limit state. Latest green remote baseline remains `ca60244` / run `29031546518`; latest checked blocked run remains `f61ab49` / `29033270673`.

## 2026-07-09T16:57Z — Codex P1 slice (ASR runtime status snapshot + UI)
- Promoted the selected ASR runtime boundary into the user/build-agent surfaces: `FirstRunStatus.asr_runtime`, `FirstRunProofPlan.asr_runtime`, recording meta, and the Screen Family 10 first-run panel now all show Pending / Blocked / VerifiedArtifact truth from Rust.
- The visible status includes selected model id, lane, runtime, artifact path, artifact byte size, adapter readiness, detail, and proof requirement. Verified artifacts still render as adapter pending until a real ASR adapter emits transcript events.
- Kept the surface scoped: no network model fetch, no Parakeet/Whisper inference claim, and no fake golden ASR output.
- Evidence saved at `ops/mission/evidence/2026-07-09-asr-runtime-status-snapshot.txt`. Local gates passed: fmt, focused ASR snapshot/proof tests, frontend check, clippy, full Rust suite (207 lib + crash/event/short suites), privacy posture, ADR status, bench PASS/PARTIAL, production build, and desktop verify.
- Remote boundary unchanged: local work remains unpushed because GitHub Actions hosted runners are still blocked before checkout by billing/spending-limit state. Latest green remote baseline remains `ca60244` / run `29031546518`; latest checked blocked run remains `f61ab49` / `29033270673`.

## 2026-07-09T16:45Z — Codex P1 slice (artifact-aware ASR runtime adapter lifecycle)
- Added an artifact-aware local ASR runtime state: Pending selected model, Blocked selected model with reason, and VerifiedArtifact with model id, lane, runtime, artifact path, and byte size. The default hotkey runtime now resolves that state from the first-run ASR selection, the source-tree model registry, and app-data model artifacts.
- Wired `select_asr_model`, `refresh_model_readiness`, and `install_model_artifact` to require an idle hotkey runtime before applying a new ASR adapter state to the active processor. Active captures refuse ASR swaps instead of changing the pipeline mid-dictation.
- Kept the boundary honest: verified artifacts now produce a precise Recognize failure saying the artifact is ready but the real runtime adapter is not implemented yet. No Parakeet/Whisper inference, model download, or network surface was faked.
- Evidence saved at `ops/mission/evidence/2026-07-09-asr-runtime-adapter-lifecycle.txt`. Local gates passed: fmt, focused ASR lifecycle tests, clippy, full Rust suite (206 lib + crash/event/short suites), frontend check, privacy posture, ADR status, bench PASS/PARTIAL, production build, and desktop verify.
- Remote boundary unchanged: local work remains unpushed because GitHub Actions hosted runners are still blocked before checkout by billing/spending-limit state. Latest green remote baseline remains `ca60244` / run `29031546518`; latest checked blocked run remains `f61ab49` / `29033270673`.

## 2026-07-09T16:32Z — Codex P1 slice (permission proof boundaries + remote SOTU pause)
- Confirmed private GitHub access is restored and current: `origin` points to `https://github.com/IslandDevCrew/kaydence.git`, active `gh` account is `IslandDevCrew`, the repo resolves as PRIVATE, and local `main` matches `origin/main` at `f61ab49`.
- Added backend-owned permission proof handoff metadata to `first_run_permission_action`: optional proof command, expected evidence, and ready boundary. Screen Family 10 now renders the command/evidence/boundary so a user or build agent can see exactly what must be proven before a permission can become ready.
- Added a preview-mode guard for permission action buttons. Browser/Vite preview now shows the desktop-runtime boundary note instead of attempting native Tauri IPC, keeping local UI smoke truthful.
- Evidence saved at `ops/mission/evidence/2026-07-09-permission-proof-boundaries.txt` plus screenshot `ops/mission/evidence/2026-07-09-permission-proof-preview.png`. Local gates passed: fmt, focused permission-action tests, frontend check, clippy, privacy posture, ADR status, full Rust suite (202 lib + crash/event/short suites), bench PASS/PARTIAL, production build, desktop verify, and Browser smoke.
- Remote CI boundary: latest Actions run `29033270673` for `f61ab49` still fails before checkout on macOS, Windows, and Ubuntu because GitHub reports failed account payments or a spending-limit increase requirement. Last known green 3-OS baseline remains `ca60244` / run `29031546518`. Pausing here with the refreshed State of the Union before continuing.

## 2026-07-09T16:19Z — Codex P1 slice (model download preflight)
- Added a Rust-owned `first_run_model_download_preflight` command for P1-P0-2/P1-P0-8. It returns model id, task, lane, runtime, file, current first-run state/detail, availability, destination path, expected sha256, size, sources, license, blocked reason, operator action, and proof requirement.
- Kept the boundary honest: the command is read-only, reuses the existing `ModelDownloadPlan`, does not fetch bytes, does not create files, and blocks placeholder hashes/sources instead of pretending a download is safe.
- Wired Screen Family 10 required-model rows with a Review action and a compact preflight card. The existing Install path remains the reviewed local-artifact import path; real network fetch/progress is still a future critical decision path.
- Evidence saved at `ops/mission/evidence/2026-07-09-model-download-preflight.txt` plus desktop/narrow screenshots. Local gates passed: fmt, focused preflight tests, frontend check, clippy, full Rust suite (202 lib + 2 crash + 5 event + 5 short), privacy/ADR gates, bench PASS/PARTIAL, production build, desktop verify/build, and Browser smoke at 1280x720 plus 390x844.
- Remote CI for `c63bc47` did not start any runner jobs: Actions run `29033116849` failed before checkout on macOS, Windows, and Ubuntu because GitHub reported failed account payments or a spending-limit increase requirement. This is a remote-infra/billing block, not a test failure.
- Honest boundary remains: live OS permission prompts/proof, approved network download/progress UI, real Parakeet/Whisper adapters, live first dictation, and <=60s reference-machine proof are still open.

## 2026-07-09T15:53Z — Codex P1 slice (first-run proof export)
- Added a Rust-owned `FirstRunProofPlan` for P1-P0-8, generated from `AppSnapshot` with schema_version, generated_at_ms, os_lane, ready_to_dictate, first_dictation_completed, next_step, setup_timing, model/ASR state, permission requirements, hotkey state, and proof_items for next step, models, permissions, hotkey, and first dictation.
- Added `export_first_run_proof_plan`, which writes app-data `exports/first-run-proof-plan.json` and returns the local path plus proof item count. Screen Family 10 now exposes a compact Export Proof action and displays the backend-returned status/path.
- Corrected the Linux AT-SPI permission proof wording so it matches the actual selftest surface: `atspi-selftest` focus tracking is the default command; `--type` is the uinput typing proof.
- Evidence saved at `ops/mission/evidence/2026-07-09-first-run-proof-export.txt` plus desktop/narrow screenshots. Local gates passed: fmt, focused proof tests, frontend check, clippy, full Rust suite (200 lib + 2 crash + 5 event + 5 short), privacy/ADR gates, bench PASS/PARTIAL, production build, desktop verify/build, and Chrome smoke at 1280x720 plus 390x844.
- Honest boundary remains: this is a local proof-plan export and build-agent handoff; it does not mark OS permissions ready, add model network download, claim first dictation, or claim <=60s reference-machine proof.

## 2026-07-09T15:30Z — Codex P1 slice (first-run next-step orchestration)
- Added backend-owned first-run sequencing for P1-P0-8. `FirstRunStatus` now carries `next_step`, recomputed after model readiness, ASR selection, permission/hotkey state, and first-dictation completion updates.
- The Rust sequence is explicit and evidence-safe: model metadata review -> reviewed model install -> OS permission proof -> hotkey recovery -> real first dictation -> complete. This does not add a network downloader, fake OS permission detection, or fake completion proof.
- Screen Family 10 now renders a compact next-step card with backend-selected title/detail/action/proof. The action dispatches the backend-selected intent when running under Tauri; Vite preview mode only shows proof guidance, avoiding noisy IPC errors in browser smoke.
- Evidence saved at `ops/mission/evidence/2026-07-09-first-run-next-step.txt`: focused next-step tests PASS, frontend check PASS, Browser desktop/narrow/click smoke PASS, clippy PASS, full Rust suite PASS (198 lib + crash/event/short suites), privacy/ADR gates PASS, bench PASS/PARTIAL, production build PASS, build/run script PASS.
- Honest boundary remains: live OS permission prompts/proof, approved model download/progress UI, a live first-dictation journey, and <=60s reference-machine proof are still open.

## 2026-07-07T00:00Z — session 1 (bootstrap)
- Bootstrapped mission-control on the docs-first Kaydence v3 scaffold (no prior ops/mission). git initialized; baseline commit of 49 tracked files.
- Seeded state.json from the v3 plan: 5 phases (P0 Toolchain, P1 MVP Dictation, P2 Cleanup, P3 Polish+Whisper-Ahead, P4 Flagship Trio), PRD IDs as task seeds, exit criteria as runnable gates.
- Reality check vs plan: repo has ZERO product code (44 md + 1 CI yml + registry.json + bench-prediction.sh). No Cargo/pnpm project, no tauri.conf. ADRs stop at 0008; PRD stops at P2-13; ROADMAP is v2 (P4=Expansion); AGENTS #5 = macOS+Windows. The v3 plan asserts ADR-0009/0010 + PRD P4-1..P4-9 as 'committed' — they are NOT in the repo. Recorded as v2->v3 drift risk; P0-T1/P0-T7 reconcile it (critical paths).
- Execution tier: inline/Agent (Tier 2-3) — user did not opt into Workflow orchestration. Host is macOS-only with no git remote: 3-OS CI + tauri-dev-on-Win/Linux gates cannot close here (blockers recorded).
- Surprise: the mission is genuinely at hour zero of P0 despite the plan's "monorepo v3 committed" framing — that count is the doc scaffold, not shippable code.
- Parked AWAITING OPERATOR GO before any P0 build work (P0-T1 and P0-T7 are critical decision paths).

## 2026-07-07T01:30Z — session 1 (P0 law)
- Operator GO: v3 supersedes v2; ratified Linux-first-class, local-only, pricing.
- Merged P0-T7 (ADRs 0009 Relay-crypto [Proposed], 0010 pricing [Accepted], 0011 Linux/3-OS [Accepted]; 0008 pricing marked superseded) and P0-T1 (reconciled AGENTS/PRD/ROADMAP/ARCHITECTURE/ci.yml to v3 3-OS; added PRD §12 flagship epics P4-1..P4-9; de-2-OS'd 9 secondary docs). 3 merges on main.
- ADR-0009 stays PROPOSED by design — Relay crypto is a critical decision path that halts for operator review before the P4-1 build. P0-G7 gate marked waived-by-design (10/11 accepted).
- Surprise: the v3 plan cites ADR-0009/0010 + PRD P4-1..P4-9 as already 'committed', but they existed only in the plan HTML — authored them here so repo == plan.
- Next: P0-T6 scripts (bench/audit-network/check-adr-status), then P0-T4 Handy study (needs network), then P0-T2 Tauri scaffold (critical path). P0 exit gates (tauri dev + CI on Win/Linux) stay waived-pending-infra on this macOS-only host.

## 2026-07-07T02:45Z — session 1 (P0 tooling + study)
- Merged P0-T6 (gate scripts) and P0-T4 (Handy study). 4/7 P0 tasks done; P0-G7 passes.
- audit-network.sh caught its own bug under test: patterns like 'fetch(' broke ERE alternation so grep errored and `|| true` swallowed it into a false PASS — a privacy gate silently passing. Switched to fixed-string (-F -e); verified it now FAILs on a planted std::net call. Also fixed bash-3.2 portability (macOS default: no `declare -A`/`mapfile`) across all three scripts.
- Handy study surprise: Handy already ships Linux (gtk-layer-shell + vulkan) — validates ADR-0011. Big adopt = TranscriptionCoordinator (single thread + 30ms debounce) as the answer to hotkey-race Pitfall P1. Big lesson = pyke ONNX AVX2 static-init crash on pre-Haswell CPUs (matters for our Win CPU-first Parakeet lane).
- Next: P0-T2 Tauri scaffold (critical path; heavy install). macOS build/test legs verifiable here; Win/Linux + tauri-dev stay waived-pending-infra.

## 2026-07-07T04:00Z — session 1 (P0 scaffold)
- Merged P0-T2 (Tauri 2.11.5 scaffold: Cargo+pnpm workspaces, React/TS/Vite front) + P0-T3 (SessionEvent contract in events.rs, 11 module stubs). 6/7 P0 tasks done.
- Verified crates.io + npm reachable; full `cargo check --all-targets` compiles (425 pkgs) on macOS. All local gates green: fmt/clippy (fixed derivable_impls)/test 4/4, frontend tsc+eslint, audit-network, adr-status.
- pnpm-11 friction: build-script approval blocked `pnpm install`/`run` (esbuild). Resolved via root package.json onlyBuiltDependencies + rebuild; added scripts/check-frontend.sh as the robust P0-G4 gate (runs local tsc/eslint binaries).
- audit-network caught a second self-bug: `surf` (HTTP crate) substring-matched "Surface" in events.rs -> false positive. Tightened patterns to `::` call-forms; re-verified it still FAILs on a planted reqwest:: call.
- P0 now blocked only by exit gates G5 (tauri dev on Win/Linux) + G6 (3-OS CI) — waived-pending-infra on this macOS-only host. Natural P0->P1 operator checkpoint after P0-T5.

## 2026-07-07T04:30Z — session 1 (P0 boundary)
- Merged P0-T5 (Linux CI system deps). ALL 7 P0 tasks done. 7 mission merges total this session.
- P0 phase set to BLOCKED — infra only. Every locally-runnable gate is green on macOS (fmt/clippy/test, frontend, audit-network, adr-status). Exit gates G5 (tauri dev Win/Linux) + G6 (3-OS CI) can't close without a GitHub remote+Actions or the reference machines (operator chose local-only).
- HALTING at the P0->P1 phase boundary per BUILD-LOOP for operator go. P1 (MVP dictation) begins the platform-critical feature work (capture/ASR/injection) — highest risk is Win UIA + Linux Wayland injection, which this host can't verify.

## 2026-07-08T00:00Z — session 2 (visual direction lock)
- Operator locked the Codex-generated Visual System v1 as the build reference: 5 logo directions + 10 screen-family boards, each rendered for macOS/Windows/Linux = 30 OS-specific views over one shared interface system. Report + assets live in the SEPARATE repo /Users/IDC2.5/Documents/Kaydence (docs/generated/ + assets/generated/kaydence-visual-pack/), not yet in this monorepo.
- Recorded the system as state.json `designDirection` (status: locked): summary, OS accent lanes (aqua/cobalt/emerald — selection accent is the only free-tier per-OS variation), the shared invariants (nav order, 8px radius, prediction-blue, ~0.5s amber-gold merge pulse, destructive confirm gate, no cloud-sync on core, a11y), the 5 logo options, and the 10 screen families mapped to their build phase.
- Wired a screen-family fidelity gate into every build phase: P1-G4 (01/03/07/10), P2-G3 (03/08), P3-G4 (02/07/08/09), P4-G-SCREENS (04/05/06). These are manual design-review gates (like P0-G5 tauri-dev) — honest: no auto-linter claimed. Noted a future scripts/check-visual-parity.sh structural check for when the frontend lands.
- Added P1-D1 (vendor the pack into the monorepo + design-reference index so the gate paths point at local files) and P1-D2 (operator picks the winning logo of 5 -> ADR-0012 visual identity; screen layout proceeds with a placeholder in the reserved slot until then).
- Extended render-sotu.mjs with a `designSection()` (reads state.designDirection) so the locked system renders first-class; re-rendered state-of-the-union.html (22 gates, 34 tasks, 8 PRs). Added the metric "Visual system (logos + screen families)".
- Authored prompts/VISUAL-BUILD-PROMPT-PACK.md — fable-safe scoped prompts (resume /goal, per-screen build, fidelity-gate review, logo-selection->ADR-0012, vendor-assets), keeping the safety core visible: owned repo, build/report-only, evidence-first, no invented claims, HALT at critical decision paths.
- Discrepancy noted (NOT fixed here, out of scope): git HEAD is on branch mission/p1-p0-4-wal with P1-P0-4 (WAL/crash-recovery) already committed, but state.json still lists P1-P0-4 as pending — that branch owes its own mission heartbeat. Left untouched so this design-direction change stays isolated.
- Scope discipline: did not merge to main (visual identity + invariant changes are operator-facing). Committed on branch mission/design-direction-lock for operator review/merge per policy.

## 2026-07-08T00:45Z — session 2 (P1-D1 vendor visual pack)
- Operator GO on P1-D1. Vendored Visual System v1 into the monorepo on branch mission/p1-d1-vendor-visual-pack (off design-direction-lock — first P1 task merged into the mission line).
- Copied 15 PNGs (5 logos + 10 screen-family boards, 18MB) from /Users/IDC2.5/Documents/Kaydence/assets/generated/kaydence-visual-pack/ to assets/brand/{logos,screens}/. Verified byte-identical: all 15 shasums match (evidence: ops/mission/evidence/P1-D1-vendor-2026-07-08.txt).
- Authored docs/design/README.md — the design-reference index: provenance + the one rule (accent-only OS variation), logo table -> ADR-0012, and family -> board -> phase -> fidelity-gate map.
- Repointed every board reference in-repo: 4 fidelity-gate commands (P1-G4/P2-G3/P3-G4/P4-G-SCREENS), the designDirection.assets pointer, the visual convention, and prompt-pack sections B/C/D. Prompt E (vendor) marked completed, kept for re-vendoring provenance.
- P1-D1 marked done in state.json (8/34 tasks). Note: .gitignore ships no image excludes, so the 18MB of boards commit cleanly; acceptable for a local-only repo, revisit (LFS?) if a GitHub remote lands.
- Next design work: P1-D2 logo selection (operator decision -> ADR-0012); prompt D in the pack preps the recommendation.

## 2026-07-08T01:00Z — session 2 (P1-D2 prep: logo review -> ADR-0012 Proposed)
- Operator GO on P1-D2 prep. Ran prompt D from the pack: visually reviewed all five vendored logo options and authored docs/decisions/0012-visual-identity.md as PROPOSED with the decision left blank — this is an operator gate; HALTED for the pick per JUDGE-AUDITOR.
- Recommendation: Option 1 K Waveform (purpose-built dark app-icon tile; chevron-K silhouette survives 16-32px; waveform->amber caret->K encodes voice->text; gold echoes the merge pulse). Runner-up: Option 4 Wordmark Forward (strongest public-site header; premium serif; lighter icon distinctiveness). Trade-off documented in the ADR, incl. a later icon-from-1 + typography-from-4 hybrid that would not reopen the ADR.
- Finding: Option 3 (Relay Monogram) embeds the Windows flag and Tux directly in the mark — third-party trademark risk; disqualified for the primary identity. Options 2/5 preserved as secondary art (privacy seal; Relay fleet illustration) — codified in the ADR so future sessions do not re-litigate.
- Gate mechanics: check-adr-status.sh would FAIL on any new non-Accepted ADR, so 0012 was added to PROPOSED_OK as the second declared by-design exception (same mechanism as 0009), with the reason string pointing at P1-D2. P0-G7 re-run: PASS (10 of 12 Accepted, 0009+0012 waived by design) — fresh evidence ops/mission/evidence/P0-G7-adr-status-2026-07-08.txt; gate record + metric updated.
- P1-D2 set to in_progress (not done — the selection itself is the operator's). designDirection.openDecision now carries the recommendation so the SOTU surfaces it.
- On operator pick: flip ADR-0012 to Accepted + record the option, remove 0012 from PROPOSED_OK, mark P1-D2 done, heartbeat. Asset production (icns/ico/hicolor sets) lands with P3 installer work.

## 2026-07-08T01:30Z — session 2 (P1-D2 done: identity locked)
- Operator reviewed the full pros/cons + confidence scores for options 1-4 (5 was self-ruled-out) and asked the key question: does splitting mark and wordmark cause design-language inconsistency? Answer recorded: no — a two-asset identity (mark + wordmark) is the standard mature-product pattern, unified here by the shared waveform motif and single palette, PROVIDED the roles are strict.
- OPERATOR DECISION: the dual-asset hybrid. Option 1 K Waveform = sole product mark (app icon, favicon, tray monochrome/template variant, installers, every isolated use). Option 4 Wordmark Forward = the Kaydence wordmark/site header; its serif is brand display type for site/marketing only. Three binding unification rules: (1) strict role separation, never composed as twin logos; (2) Option 4's standalone icon variant is RETIRED — one icon only; (3) serif never inside the app UI (app stays on the boards' sans).
- ADR-0012 flipped to Accepted with the decision + rules recorded; 0012 removed from check-adr-status.sh PROPOSED_OK (0009 again the single by-design exception). P0-G7 re-run: PASS, 11 of 12 Accepted — fresh evidence ops/mission/evidence/P0-G7-adr-status-2026-07-08.txt (overwrites the morning run; both runs PASS).
- state.json: P1-D2 done (9/34 tasks), logoOptions annotated (1+4 SELECTED, 3 REJECTED for third-party marks, 2/5 secondary art), identity added to designDirection.invariants, openDecision RESOLVED, conventions + resume updated. Brand-invariant violations (serif in app UI, second icon, mark/wordmark substitution) now fail the phase fidelity gates by convention.
- Follow-on work (not started): produce multi-resolution icon sets from the Option 1 mark (macOS .icns, Windows .ico, Linux hicolor PNGs + symbolic tray) with the P3 installer task; adopt Option 4's serif for the public site when site work begins.

## 2026-07-08T04:15Z — session 2 (merge train to main + CI green-up + P1 prompt)
- BIG STATE SHIFT: the two standing "high" risks (no git remote, macOS-only host) are RESOLVED — origin=github.com/IslandDevCrew/kaydence with 3-OS Actions CI went live during the WAL session. Reconciled state.json to reality: repo URL, merge policy (CI = Win/Linux trust anchor), P1-P0-4 marked done (PR #1, crash_recovery 2/2 macOS), phase P1 -> in_progress, activePhase P0 -> P1.
- Diagnosed both first-CI-run failures (main push 28913537682 + PR #1 28913731901, all three legs red): (1) install step — pnpm 11.10 no longer reads onlyBuiltDependencies/package.json 'pnpm' field; approvals moved to `allowBuilds:` in pnpm-workspace.yaml. Reproduced locally with clean node_modules, fixed (allowBuilds.esbuild=true), verified frozen-lockfile install + check-frontend PASS. (2) Windows tauri-build — 'icons/icon.ico not found'. Generated the full icon set from the ADR-0012 Option 1 K Waveform mark (auto-cropped tile, 22.5% rounded alpha, icon.png/32/128/256 + multi-res .ico + iconutil .icns); bundle.icon updated; cargo check green. Both fixes on branch mission/ci-green-pnpm-icon w/ evidence CI-GREEN-2026-07-08.txt.
- MERGE TRAIN to main (operator go): four --no-ff merges in order — design-direction-lock (carries WAL 4f1b27c), p1-d1-vendor, p1-d2-adr0012, ci-green-pnpm-icon. Also committed the P0-G6 CI watch-snapshots the WAL session left uncommitted. main HEAD = bc93326; tree clean.
- Authored prompts/P1-CAPTURE-INJECTION-PROMPT.md — the fable-safe build prompt for P1-P0-1 (hotkey capture: TranscriptionCoordinator single-thread+30ms debounce, PTT+toggle, 250ms/300ms, WAL-before-ASR) and P1-P0-3 (universal injection: Wayland-spike-FIRST, then X11/macOS-AX/Win-UIA, one TextInjector trait, clipboard snapshot-restore <=200ms, and secure-field refusal as a hard per-OS tested invariant). Security bar written as tested invariants; every dep/platform decision routed through audit-network + ADR + operator HALT.
- Icon note: the icons committed are a DEV-GRADE programmatic crop of the Option 1 board (good enough to make CI + the app window correct); production-grade multi-res icon polish + Linux hicolor/tray template stays with P3 installers per ADR-0012.
- Next: push main -> confirm first green 3-OS run -> close P0-G6 with the run URL. Then run the P1 prompt (P1-P0-1 and the P1-P0-3 Wayland spike).

## 2026-07-08T09:27Z — session 2 (P0-G6 CLOSED — first 3-OS CI green)
- Pushed main (281d897); CI run 28931588048 came back GREEN on all three legs: macos 2m20s, ubuntu 5m31s, windows 9m48s. This is the P0 exit gate that had been the mission's #1 blocker since bootstrap.
- Closed P0-G6 (3-OS CI) and P0-G3 (3-OS backend test — now actually verified on Win+Linux, not just macOS) with evidence ops/mission/evidence/P0-G6-3os-green-2026-07-08.txt (run URL + SHA + per-leg timings). Metric "Platform parity (CI legs green)" 1/3 -> 3/3.
- P0 is down to a SINGLE open item: P0-G5 (windowed `cargo tauri dev` visually confirmed on Win+Linux), which is a hardware check headless CI cannot do — needs the reference machines. Everything else in P0 is green. Blocked-reason + blockers reduced accordingly.
- Net: the two long-standing "high" infra risks (no remote; macOS-only host) are gone; one medium hardware check remains. The repo now builds, tests, and gates green on all three OSes on every push. Clear runway for the P1 input-layer build.

## 2026-07-08T05:00Z — session 2 (P1-P0-3 Wayland injection spike)
- Reference-lane clarification (operator asked): macOS = this host (build/gate here; windowed visual check is operator's eyes). Linux = the UTM Debian 12 VM CAN be the reference but is NOT reachable now (UTM gateway 192.168.64.1 silent, no guest in arp — VM off/host-only). To use: start it + `apt install openssh-server` + `systemctl enable --now ssh`; Debian 12 GNOME = Mutter = the HARD/representative Wayland lane (portal/libei, not virtual-keyboard). Windows = NO handoff needed; already fully covered by CI on every push; only the windowed visual check + hands-on injection need a real box, deferred.
- Ran the P1 prompt's Phase-2a Wayland spike. Deliverables: docs/spikes/P1-P0-3-wayland-injection.md (compositor matrix wlroots/GNOME/KDE/uinput/X11; capability-ladder strategy; the secure-field gap on opaque Wayland clients; what's testable on host vs VM vs Windows; dep list; VM validation plan) + ADR-0013 (Proposed).
- Implemented the platform-AGNOSTIC injection core in inject/mod.rs (replacing the P0-T3 stub): FieldKind + UnknownFieldPolicy + decide_secure (secure-field gate, non-negotiable #8); KeystrokeChannel/InjectorCaps/InjectPlan + select_plan (native-first; clipboard needs a keystroke path — a real Wayland nuance); Clipboard trait + clipboard_paste (snapshot always restored, even on paste failure — Pitfall P2); InjectFailure + outcome_event (-> Injected/Held/Failed); TextInjector trait + UnimplementedInjector placeholder (reports no caps, refuses — nothing silently succeeds). 13 unit tests pass; fmt+clippy(-D warnings)+audit-network(0 sites) green; full backend suite 20 tests green on host, to be re-verified on 3-OS CI.
- Honest core finding: Wayland has NO global secure-field signal for opaque (non-AT-SPI) clients. Rather than fake a guarantee, decide_secure encodes it: Lenient (default) injects but flags verified:false (UI warns per app, board 07); Strict refuses opaque focus. Recorded as a medium risk + in ADR-0013.
- ADR-0013 PROPOSED (critical decision path: new OS-input surfaces + new crates). Added 0013 to check-adr-status PROPOSED_OK (reason -> P1-P0-3); P0-G7 re-run PASS (11/13 accepted, 0009+0013 by-design). NO deps added yet — syscall bodies stay Err(ADR-0013 pending) until operator go, then implemented + validated on the VM.
- P1-P0-3 -> in_progress (spike done; implementation gated on operator go + VM). HALTING per JUDGE-AUDITOR for: (1) operator approval of the ADR-0013 dependency set, (2) the VM being brought up for live validation. Per the operator's sequence, Phase 1 hotkey capture is the next unblocked build — but its global-shortcut plugin is itself a dep-add (audit-network + ADR), so it also wants an operator go on the P1 platform dependency set.

## 2026-07-08T05:40Z — session 2 (P1-P0-1 hotkey capture core)
- Spike passed with no issues, so per the operator's sequence proceeded to Phase 1. Built the platform-agnostic capture CORE (hotkeys/mod.rs, replacing the P0-T3 stub): CaptureCoordinator = the single serialized owner (Handy TranscriptionCoordinator shape, ADR-0004) + CaptureState machine (Idle/Capturing/Finalizing — illegal transitions unrepresentable). PTT + Toggle modes; CaptureConfig defaults 250ms min-capture / 300ms tail / 30ms debounce.
- Modelled as a pure state machine (time injected as u64 ms; Signal Press/Release/Tick -> Action Start/Finalize/Discard/None) so the Pitfall-P1 guarantees are unit-tested with no threads or real clock: debounce blocks double-start; sub-250ms taps discarded; 300ms tail keeps capturing after release (no clipped audio); early re-press during finalize cannot overlap sessions. 6 tests; fmt+clippy(-D)+audit-network(0) green; backend suite now 26 tests. Merged to main (ee372a2).
- No new dependency added, so no ADR gate for the core — clean merge. The dep-gated REMAINDER of P1-P0-1 is the OS hotkey registration (tauri global-shortcut plugin) + wiring the coordinator to audio/WAL.
- CONSOLIDATED OPERATOR DECISION now on the table (state.resume): approve the P1 platform dependency set to unblock BOTH tracks at once — global-shortcut (hotkey) + atspi/ashpd+libei/wayland-client/x11rb/wl-clipboard-rs (injection, ADR-0013). All non-network; each is a per-add audit-network review + ADR. Then wire the tested cores to the OS + audio/WAL and live-validate on the Debian-12 GNOME VM.
- Pattern established this session: for each highest-risk platform task, land the pure decision/coordination CORE (fully host-tested, CI-green, no deps) first, then gate the OS plumbing on an operator dep decision + reference-machine validation. Two tested cores now on main (injection + hotkey); zero unverified platform claims.

## 2026-07-08T06:20Z — session 2 (Linux reference machine online)
- Operator brought the UTM Debian VM up. Connected from the host: ssh debian@192.168.64.4 (expect-wrapped password; StrictHostKeyChecking=accept-new). Env = Debian 12 bookworm, ARM64, graphical session Type=wayland (GNOME/Mutter — exactly the hard/representative Wayland lane the spike targets: portal path, not wlroots). sudo = root OK.
- Set the VM up as the Linux reference: apt build deps (gtk-3, webkit2gtk-4.1, appindicator, rsvg2, xdo, at-spi2-core + libatspi2.0-dev, ssl, pkg-config, rsync), rustup stable 1.96.1, repo synced via rsync (source only). cargo test --lib -> 26 passed / 0 failed on ARM64 Debian — a real reference-hardware pass beyond CI's x86 Ubuntu leg (evidence P1-linux-vm-reference-2026-07-08.txt). Both new modules present (inject 464 lines, hotkeys 263).
- Windows box: operator has one booting. Guidance recorded — do NOT block on it now: nothing Windows-hands-on is testable until the injection/hotkey OS backends exist (dep-gated), and CI already covers Windows compile/test on every push. Windows becomes valuable at the LATER step (windowed check + UIA/SendInput injection pass), after the Linux-first backend build/validation. Leave it on if convenient; no rush.
- Path forward is unchanged and now fully unblocked on the operator side EXCEPT the one gate: approve the P1 platform dependency set (global-shortcut plugin + ADR-0013 injection crates). On go: implement backends (Linux AT-SPI/portal FIRST — highest risk, VM is ready), build headless on the VM over SSH, then operator-observed live injection validation on the VM screen; then macOS (host) + Windows backends + the Windows hands-on pass.

## 2026-07-08T07:00Z — session 2 (operator GO: ADR-0013 accepted; dev-command fix; VM window)
- Operator said GO into steps 1-3 = approval of the P1 platform dependency set. ADR-0013 flipped to Accepted; removed from check-adr-status exceptions (0009 again the only by-design exception); P0-G7 PASS. Merged to main (4b772de) + pushed.
- P0-G5 root cause found + fixed: `cargo tauri dev` never worked because beforeDevCommand was `pnpm dev` run at the workspace root, which has no `dev` script (ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL). Fixed to `pnpm --filter kaydence-desktop dev` (+ beforeBuildCommand). Verified on the VM: `pnpm --filter kaydence-desktop dev` -> VITE ready on :1420. vite.config already had strictPort 1420 matching devUrl. This unblocks the windowed check on ALL platforms.
- VM setup for the window check: pnpm 9.15.9 installed globally, cargo-tauri v2.11.4 installed, GNOME toolkit-accessibility enabled (for later AT-SPI). Auto-launching the GUI detached over expect-SSH proved unreliable (setsid/-f/nohup through the expect wrapper didn't persist the process) — a harness limitation, not a repo issue. Handoff: operator runs `cd ~/kaydence && cargo tauri dev` in the VM's GNOME terminal to confirm the Linux window (operator-observed, same as the macOS P0-G5 leg).
- HONEST STATUS on steps 1-3: foundation is solid and real (ADR-0013 accepted, deps approved, both cores tested + building on ARM64 Debian, dev-command fixed). The actual platform backends (AT-SPI secure-detection + native insert + portal/libei keystrokes on Linux; global-shortcut registration; macOS AX; Windows UIA) are genuine multi-iteration work that needs the VM compiler in the loop + operator-observed live validation — NOT completed this session. Not faked. Next focused effort: implement the Linux injection backend against the VM (build over SSH, iterate, then operator-observed validation), keeping the non-negotiable #8 secure-field gate intact.
- Windows box guidance reaffirmed: no value yet (no backend to hands-on-test); CI covers compile/test. Its moment is after the backends exist.

## 2026-07-08T08:00Z — session 2 (steps 1+2: VM window running + Linux AT-SPI validated)
- STEP 1 (P0-G5 Linux window): launched `cargo tauri dev` on the VM in a persistent tmux session 'kdev' (the reliable detach — nohup/setsid over expect-SSH kept dying). App built + ran: vite on :1420, /home/debian/kaydence/target/debug/kaydence PID alive/stable (GTK software-rendering fallback, no GPU in VM — benign). Window is up on the VM's GNOME screen for operator visual confirm. Evidence P0-G5-linux-window-2026-07-08.txt.
- STEP 2 (P1-P0-3 Linux injection backend, first validated increment): added atspi 0.30 + pollster + zbus 5 (cfg linux; pure Rust over D-Bus — NOT network, audit-network PASS; no libatspi C dep so CI Ubuntu builds it clean). inject/linux.rs: role_to_field_kind (AT-SPI Role -> FieldKind, PasswordText=>Secure regardless of editable — non-negotiable #8), 3 tests. bin/atspi-selftest: live harness.
- Built + ran on the VM, iterating the atspi 0.30 API over 3 compile passes (ObjectRef name/path are methods not fields; name() is Option; simplified enumeration to a single clean call after a child_count() signature quirk). RESULT: connect -> AccessibleProxy(root) -> get_role() = DesktopFrame -> PASS. The full AT-SPI stack works on real GNOME/Wayland — the hardest de-risking (does the stack even work on target) is DONE. Evidence P1-P0-3-atspi-validated-2026-07-08.txt. Committed 4be669c.
- Honest scope: this is the FOUNDATION (deps validated + connect + role mapping), not the finished injector. Remaining (next iteration): focus tracking via object:state-changed:focused, read the focused element's role/state for live secure detection, EditableText.insert_text for native insertion, then operator-observed live validation (type into gnome-text-editor; refuse a password field). Then macOS AX + Windows UIA backends + hotkey OS registration. Each builds on proven patterns; no unverified claims made.

## 2026-07-08T09:30Z — session 2 (focus-tracking + insertion: strategy-revising findings)
- Built out bin/atspi-selftest into a real harness: focus-tracking (register object:state-changed, filter Focused+enabled -> read role/state -> classify -> insert or refuse), plus --direct (tree-walk to an app's editable field and insert) and --read (read a field's text back). Iterated the atspi 0.30 API over several VM compile passes (Event/ObjectEvents paths from event_wrappers; StateChangedEvent.enabled is bool; ObjectRef name()->Option<&UniqueName>/path()->&ObjectPath; ObjectPath->OwnedObjectPath via .into()).
- LIVE VALIDATION on the GNOME VM produced three evidence-based findings that REVISE the strategy:
  1. AT-SPI connect + traverse + READ work: --direct gnome-text-editor found 'role=Text -> Editable'; character_count/get_text round-trip fine. Detection (role/state) works.
  2. AT-SPI EditableText.InsertText is UNRELIABLE on modern GNOME: returns true but readback stays empty on BOTH gnome-text-editor (GTK4) AND gedit 44. GTK4's accessibility backend treats these as effectively read-only. => AT-SPI is our DETECTION / secure-field signal, NOT the primary insertion path. (Directly validates non-negotiable #8's refusal signal — the part that works.)
  3. uinput keystroke synth works: ydotool runs daemonless on /dev/uinput (exit 0). Keystroke-below-the-compositor actually delivers text on GNOME/Wayland.
- Revised the capability ladder in docs/spikes §7b + an ADR-0013 amendment: DETECT via AT-SPI, INSERT via keystroke (uinput or portal+libei); AT-SPI native insert is a best-effort bonus only. Evidence P1-P0-3-atspi-live-findings-2026-07-08.txt. Merged to main (f018b26).
- Honest limit hit: the end-to-end 'keystroke types into the focused editor' auto-proof is blocked by Wayland's focus model — a script-launched window doesn't reliably get keyboard focus, and Wayland won't let a script move focus (the same isolation the whole ADR is about). So that final check is operator-in-the-loop: operator focuses the target, the injector types, AT-SPI reads it back. Did not fake it.
- NEXT: implement the uinput (and/or portal+libei) keystroke backend behind the existing KeystrokeChannel enum, gated by the AT-SPI secure-field detection; then macOS AX + Windows UIA + hotkey registration. This spike turned an assumption (AT-SPI native insert) into a validated architecture change — exactly its purpose.

## 2026-07-08T10:30Z — session 2 (uinput keystroke backend built + validated)
- Built inject/uinput.rs: a pure US-QWERTY char->keycode map (letters/digits/symbols/space/enter/tab; raw Linux input-event-codes so it's host/CI-testable — 5 unit tests) + UinputKeyboard (evdev 0.13) that creates a /dev/uinput virtual keyboard and types text as key events below the compositor. Iterated the evdev 0.13 API on the VM (InputEvent::new(type_:u16,...) not EventType; KeyCode not Key; VirtualDevice::builder() not the deprecated VirtualDeviceBuilder::new()).
- VALIDATED the write path live on the VM (as root for /dev/uinput access): `--type 'Kaydence_uinput_123'` -> keymap 19/19 typable, virtual keyboard created at /dev/input/event5 (a REAL kernel input device), emitted 19 keystroke chars PASS; also a 26-char string. So uinput keystroke synthesis works on GNOME/Wayland — the validated PRIMARY insertion path.
- Wired the REAL integrated backend into atspi-selftest focus-track mode: AT-SPI decides WHETHER to inject (secure-field gate, non-negotiable #8), uinput does the TYPING (ASCII marker; AT-SPI InsertText no-ops on modern GNOME). Added scripts/linux-inject-demo.sh (installs a udev rule for input-group /dev/uinput access, builds, runs the harness) for the operator live demo.
- Nuances found: /dev/uinput resets to root-only without a persistent udev rule (the demo script installs one); the readback once showed an AT-SPI marker DID land ("[Kaydence" partial), so GTK4's InsertText is inconsistent-not-absolute — reinforces keystroke-as-primary. Also confirmed: unicode beyond US-QWERTY (e.g. ✓) isn't typable via raw keycodes yet — documented follow-up (compose/keymap-swap).
- HONEST wall (again, by Wayland design): the fully-automated 'keystroke lands in a focused editor + reads back' proof is blocked because a script can't move keyboard focus on Wayland and SSH-launched GUI focus/a11y-registration is flaky. Every PIECE is validated individually (device+emit, keymap, AT-SPI detect+read); the integration is the operator's 2-minute check via the demo script. Did not fake it. Merged to main (8ee496a).
- NEXT: operator runs scripts/linux-inject-demo.sh for the live it-types/it-refuses moment; then unicode typing, the portal+libei alternative (no device perm), macOS AX, Windows UIA, hotkey registration.

## 2026-07-08T22:29Z — Codex recovery pass (UI shell + handoff + run action)
- Re-oriented from the empty `/Users/IDC2.5/Documents/Kaydence` staging repo to the real app repo at `/Users/IDC2.5/Kaydence/kaydence`. Verified the remote problem is current: `git fetch origin` returns `Repository not found` for IslandDevCrew/kaydence, so local main is authoritative until the operator restores/recreates/repoints the remote.
- Fresh local gates re-run and recorded in `ops/mission/evidence/2026-07-08-codex-recovery-ui-and-gates.txt`: fmt PASS, clippy PASS, backend tests PASS (28 incl. crash recovery), frontend check PASS, audit-network PASS (0 sites), ADR status PASS (0009 only by-design Proposed), and `./script/build_and_run.sh build` PASS.
- Replaced the P0 placeholder React app with a presentation-only P1 cockpit/setup shell aligned to screen families 01, 03, and 10: left nav order, 8px cards, selected Option 1 mark, macOS/Windows/Linux accent switcher, cleanup dial, injection status, first-run checklist, and local-only posture. React still contains no product logic; it renders static/presentational state only.
- Browser smoke: Vite dev server at 127.0.0.1:1420 rendered in system Chrome via Playwright. Verified h1 `Dictation cockpit`, active macOS lane, 10 nav items, 4 metric cards, favicon present, and zero console/page errors.
- Added `script/build_and_run.sh` plus `.codex/environments/environment.toml` so Codex has a Run action for the Tauri app. Added `docs/CODEX_SUPER_GOAL_PROMPT.md`, `docs/WINDOWS_CODEX_HANDOFF_2026-07-08.md`, and `docs/KAYDENCE_CURRENT_STATE_OF_UNION_2026-07-08.md` to stabilize continuation and Windows handoff.
- Cleaned Vite generated `dist/` artifacts after verification; source remains the durable change. Next high-leverage build remains P1 OS wiring: global-shortcut registration into the tested coordinator, macOS AX injection, Windows UIA/SendInput on real Windows, and Linux live demo closure.

## 2026-07-08T22:36Z — Codex P1 slice (Rust-owned settings snapshot)
- Advanced P1 without adding new platform dependencies: replaced the settings P0 stub with typed Rust-owned defaults, validation, first-run readiness, and a serializable `AppSnapshot`. Defaults now encode the product thesis: push-to-talk, Parakeet CPU, Light cleanup, Full opt-in, 250/300/30ms capture timing, 30-day retention, local context/OCR off, and unknown-focus warning policy for opaque clients.
- Added `app_snapshot` as a Tauri command in `lib.rs`, giving the presentation layer a backend-owned config source without moving product logic into React.
- Added five settings tests (defaults, JSON round-trip, bad schema/empty hotkey validation, privacy validation, first-run readiness), raising backend coverage to 33 total tests.
- Fresh evidence saved at `ops/mission/evidence/2026-07-08-p1-settings-snapshot.txt`: fmt PASS, clippy PASS, backend tests PASS (33 incl. crash recovery), frontend check PASS, audit-network PASS, ADR status PASS, and `./script/build_and_run.sh build` PASS. No new deps, no network surface.

## 2026-07-08T22:41Z — Codex P1 slice (frontend consumes backend snapshot)
- Added the official Tauri v2 frontend API package `@tauri-apps/api` and wired the React shell to call `invoke<AppSnapshot>("app_snapshot")` from `@tauri-apps/api/core`, matching current Tauri v2 docs. This gives the UI a real backend-owned config source while keeping product logic in Rust.
- Kept a local preview fallback for ordinary Vite/browser smoke tests outside the Tauri runtime. The fallback mirrors the Rust defaults but is explicitly marked `preview config` in the UI; in Tauri, the command can switch the badge to `backend config`.
- UI now renders snapshot-derived app name, engine, hotkey binding, cleanup default, retention/context posture, and unknown-focus policy instead of hardcoded display constants.
- Evidence saved at `ops/mission/evidence/2026-07-08-frontend-app-snapshot-invoke.txt`: backend tests PASS (33), frontend check PASS, audit-network PASS (0 sites), ADR status PASS, `./script/build_and_run.sh build` PASS, and browser smoke PASS in system Chrome with zero console/page errors.

## 2026-07-08T22:46Z — Codex P1 slice (global-shortcut registration)
- Added the official Tauri v2 global-shortcut plugin crate for desktop targets and registered the default RightAlt binding during Tauri setup. The handler feeds pressed/released events into the existing tested `CaptureCoordinator` with push-to-talk defaults and logs coordinator actions locally.
- This is a deliberately narrow OS wiring increment: it proves the plugin dependency compiles, the app starts with registration installed, and shortcut events have a path into the tested coordinator. It does **not** yet connect `StartCapture`/`FinalizeCapture` to audio/WAL, and real keypress behavior still needs manual OS-level validation.
- Evidence saved at `ops/mission/evidence/2026-07-08-global-shortcut-registration.txt`: fmt PASS, backend tests PASS (33), clippy PASS, frontend check PASS, audit-network PASS (0 sites), ADR status PASS, `./script/build_and_run.sh build` PASS, and macOS `./script/build_and_run.sh --verify` PASS with cleanup.

## 2026-07-08T22:53Z — Codex P1 slice (hotkey -> WAL runtime bridge)
- Added `audio::WalCaptureRuntime`, which owns one active app-data `sessions/{ulid}.wav` WAL per dictation: `StartCapture` opens/fsyncs the WAV header, `DiscardCapture` removes short-tap WALs, and `FinalizeCapture` patches/fsyncs the header and produces an `AudioPersisted` event payload for downstream history/engine wiring.
- Replaced console-only global-shortcut action logging with a shared `HotkeyRuntime` in `lib.rs`: RightAlt press/release feeds the existing `CaptureCoordinator`, valid releases schedule a 300 ms tail tick, and runtime actions now start/discard/finalize the WAL lifecycle. Added `CaptureCoordinator::reset()` so WAL-open failure cannot strand the pure state machine in `Capturing`.
- Honest boundary: this closes the prior audio/WAL connection caveat at the session-file lifecycle level, but it does **not** claim microphone samples are captured yet; cpal/ring-buffer draining into `WalWriter::append` remains the next P1 audio slice. Real keypress behavior still needs manual OS-level validation across macOS/Windows/Linux.
- Evidence saved at `ops/mission/evidence/2026-07-08-hotkey-wal-runtime-bridge.txt`: fmt PASS, backend tests PASS (40 incl. crash recovery), clippy PASS, frontend check PASS, audit-network PASS (0 sites), ADR status PASS, `./script/build_and_run.sh build` PASS, and macOS `./script/build_and_run.sh --verify` PASS with cleanup.

## 2026-07-08T23:03Z — Codex P1 slice (cpal ring-buffer capture drain)
- Added the first real mic-capture path: `cpal` 0.17.x opens the default input stream on `StartCapture`, after choosing a supported 16 kHz PCM input config; `ringbuf` 0.5 provides the lock-free SPSC boundary from callback to drain thread. Both are local audio dependencies and audit-network stayed at 0 call sites.
- The realtime callback now only downmixes interleaved PCM frames into f32 mono and `try_push`es into the ring, counting overflow via an atomic. The drain thread owns `WalWriter` and appends samples to disk, so WAL writes remain off the cpal callback path. `FinalizeCapture` stops the stream, joins the drain, patches/fsyncs the WAV header, and reports dropped input sample count.
- Added CI-safe tests for callback downmix/overflow and drain-thread WAL append; tests use `WalCaptureRuntime::new_wal_only` so microphone permission is not required. Honest boundary: this builds the production cpal path and proves ring-to-WAL behavior, but a manual RightAlt+mic recording on macOS/Windows/Linux and a rubato fallback for non-16-kHz devices remain follow-up P1 validation/build slices.
- Evidence saved at `ops/mission/evidence/2026-07-08-cpal-ring-wal-capture.txt`: fmt PASS, backend tests PASS (42 incl. crash recovery), clippy PASS, frontend check PASS, audit-network PASS (0 sites), ADR status PASS, `./script/build_and_run.sh build` PASS, and macOS `./script/build_and_run.sh --verify` PASS with cleanup.

## 2026-07-09T04:16Z — Codex P1 slice (model readiness recheck)
- Added `refresh_model_readiness` as a backend-owned Tauri command that rescans app-data model artifacts through the same `RuntimeSnapshot::refresh_models_from_app_data` helper used at startup, then reapplies any persisted ASR selection before returning a fresh `AppSnapshot`.
- Added the first-run `Recheck Models` action in React. The frontend keeps presentation-only boundaries: it holds only a pending button state, sends the command, and renders the backend snapshot. It does not compute model readiness.
- Added `runtime_snapshot_rechecks_models_after_artifact_install`: missing fixture ASR/VAD artifacts report Missing, then the same runtime flips to Ready after the artifacts are written and the refresh helper runs again. This closes the no-restart setup loop for build agents/users placing reviewed files into app-data.
- Evidence saved at `ops/mission/evidence/2026-07-08-model-readiness-recheck.txt`: fmt PASS, focused recheck test PASS, runtime_snapshot suite PASS (8), frontend check PASS, backend tests PASS (151 lib + 2 crash recovery), clippy PASS, audit-network PASS (0 sites), ADR status PASS, npm build PASS, `./script/build_and_run.sh build` PASS, and `./script/build_and_run.sh --verify` PASS with generated dist cleanup. Honest boundary unchanged: no downloads, no progress UI, no real artifact hashes/sources, no Parakeet/Whisper adapters, and no WER/latency evidence yet.

## 2026-07-09T04:22Z — Codex P1 slice (ASR pending runtime adapter)
- Replaced the default runtime's empty ASR stack with a `PendingLocalAsrEngine`, so hotkey captures now hit WAL/history and then record a specific Recognize failure for the selected local ASR model while the real Parakeet/Whisper adapters are still pending.
- Expanded `AsrError::AllEnginesFailed` to retain per-lane failure summaries. Empty stacks still display the old generic message, but real configured failures now include context such as `local_cpu: engine unavailable: local ASR adapter is not loaded yet for selected model 'fixture-asr'`.
- Threaded first-run's selected ASR model/lane into `HotkeyRuntime::new` at startup. This intentionally does not fake recognition or claim first successful dictation; it makes the current runtime failure mode precise and auditable until the real adapter lifecycle lands.
- Evidence saved at `ops/mission/evidence/2026-07-08-asr-pending-runtime-adapter.txt`: fmt PASS, focused tests PASS, engine suite PASS (7), pipeline suite PASS (9), backend tests PASS (154 lib + 2 crash recovery), clippy PASS, frontend check PASS, audit-network PASS (0 sites), ADR status PASS, npm build PASS, `./script/build_and_run.sh build` PASS, and `./script/build_and_run.sh --verify` PASS. Remote truth rechecked: origin fetch still returns `Repository not found`.

## 2026-07-09T04:28Z — Codex P1 slice (recent history surface)
- Added a read-only `recent_history` Tauri command that opens the app-data SQLite history store and returns a bounded recent session list. `HistorySession`/`HistoryFailure` now serialize for the frontend, with a test covering the failure shape.
- Wired the cockpit to load and manually refresh recent local history. The new surface renders target app, clean/raw/failure/audio summary, and a compact status under the main dictation panel, so WAL/history is visible rather than hidden in the database.
- Kept the frontend boundary intact: React renders backend rows and sends a refresh intent only; it does not own canonical history state or mutate text.
- Evidence saved at `ops/mission/evidence/2026-07-08-recent-history-surface.txt`: fmt PASS, history serialization test PASS, frontend check PASS, backend tests PASS (155 lib + 2 crash recovery), clippy PASS, audit-network PASS (0 sites), ADR status PASS, npm build PASS, `./script/build_and_run.sh build` PASS, and `./script/build_and_run.sh --verify` PASS. Honest boundary: no audio playback/export/purge/retention sweep/crash-orphan recovery UI yet.

## 2026-07-09T04:35Z — Codex P1 slice (history session delete)
- Added backend-owned single-session deletion: `HistoryStore::delete_session_and_audio` removes the SQLite row and removes audio only when the stored path canonicalizes under app-data `sessions/`, refusing relative, missing, non-file, symlink-escaped, and external paths.
- Exposed `delete_history_session` as the Tauri intent. The command opens app-data history, runs the safe delete, then returns the refreshed recent list. The cockpit now renders a per-row Delete action with a confirm gate and pending state; React still does not mutate SQLite or files.
- Added tests for the two important safety outcomes: normal app-data WAL file deletion removes both DB row and audio, while an external audio path leaves the file untouched and only removes the history row.
- Evidence saved at `ops/mission/evidence/2026-07-08-history-session-delete.txt`: fmt PASS, focused delete tests PASS (2), frontend check PASS, state JSON PASS, backend tests PASS (157 lib + 2 crash recovery), clippy PASS, audit-network PASS (0 sites), ADR status PASS, npm build PASS, `./script/build_and_run.sh build` PASS, `./script/build_and_run.sh --verify` PASS, dist cleanup, and no lingering app/dev-server processes. Honest boundary: no audio playback/export/purge-all/retention sweep/crash-orphan recovery UI yet.

## 2026-07-09T04:43Z — Codex P1 slice (history orphan recovery surface)
- Wired `recent_history` through a backend-owned orphan scan before it lists rows. It now looks under app-data `sessions/`, parses ULID-named WAVs, skips sessions already represented in SQLite, patches recoverable WAL headers in place, and records untracked files as `AudioPersisted` + `Failed { stage: Capture }`.
- Added `HistoryStore::record_recovered_audio` and `session_has_audio`. Recovered audio becomes visible in the cockpit as a failed Capture session with the preserved audio path, instead of remaining an invisible file after a crash or app exit before transcription.
- Added focused coverage for orphan surfacing and idempotence: orphan WAL -> history row, started-but-not-audio row -> recovered row without losing target app, and `recent_history` recovery bridge -> one surfaced row only.
- Evidence saved at `ops/mission/evidence/2026-07-08-history-orphan-recovery-surface.txt`: fmt PASS, focused recovery tests PASS (3), frontend check PASS, backend tests PASS (160 lib + 2 crash recovery), clippy PASS, audit-network PASS (0 sites), ADR status PASS, state JSON PASS, npm build PASS, `./script/build_and_run.sh build` PASS, `./script/build_and_run.sh --verify` PASS, dist cleanup, and no lingering app/dev-server processes. Honest boundary: recovered audio is surfaced but not re-transcribed until real Parakeet/Whisper adapters land.

## 2026-07-09T04:51Z — Codex P1 slice (history export)
- Added backend-owned per-session export: `HistoryStore::export_session` writes app-data `exports/{session}.json` and `.txt`, using the existing SQLite summary plus the ordered `SessionEvent` stream. Missing sessions return `exported=false`; invalid ULIDs are rejected at the Tauri command boundary.
- Exposed `export_history_session` as a Tauri intent and added a cockpit Export action per history row. React only sends the intent and renders the returned path/status; it does not construct files, transform text, choose destinations, or mutate history state.
- Added focused tests for both outcomes: real sessions write JSON/text under app-data with raw/clean/audio/event data, and missing sessions do not create an exports directory.
- Evidence saved at `ops/mission/evidence/2026-07-08-history-export-session.txt`: fmt PASS, focused export tests PASS (2), frontend check PASS, backend tests PASS (162 lib + 2 crash recovery), clippy PASS, audit-network PASS (0 sites), ADR status PASS, state JSON PASS, npm build PASS, `./script/build_and_run.sh build` PASS, `./script/build_and_run.sh --verify` PASS, and dist cleanup. Honest boundary: audio playback, purge/delete-all, retention sweep, recovered-audio re-transcription, and 3-OS validation remain.

## 2026-07-09T05:05Z — Codex P1 slice (history purge and retention)
- Added backend-owned purge and retention cleanup. `HistoryStore::purge_all` removes all SQLite sessions/events, app-data `sessions/` files, and app-data `exports/` files. `HistoryStore::sweep_retention` removes expired rows plus their safe audio/export artifacts, and `recent_history` now applies the Rust settings retention window before recovery/listing.
- Strengthened single-session deletion: `delete_session_and_audio` now removes matching app-data export files as well as the DB row and safe session audio, while still refusing external audio paths.
- Added `purge_history` as the Tauri intent and a cockpit `Purge All` action with a confirm gate. React renders returned counts and never decides which files are history.
- Evidence saved at `ops/mission/evidence/2026-07-08-history-purge-retention.txt`: fmt PASS, focused history tests PASS (16), frontend check PASS, backend tests PASS (165 lib + 2 crash recovery), clippy PASS, audit-network PASS (0 sites), ADR status PASS, state JSON PASS, npm build PASS. Honest boundary: audio playback, recovered-audio re-transcription, and 3-OS gate evidence remain.

## 2026-07-09T05:18Z — Codex P1 slice (history audio playback)
- Added backend-owned audio playback preparation. `HistoryStore::audio_playback` returns a playable asset only when the stored audio path canonicalizes under app-data `sessions/`, is a WAV, and is a real file; external or unsafe paths return `None`.
- Enabled Tauri's scoped asset protocol for `app-data/sessions/**` and added `media-src` CSP entries for `asset:` / `http://asset.localhost`. This pulled in Tauri's `http-range` helper through the lockfile for local media serving, while `scripts/audit-network.sh` still reports 0 app network call sites.
- Added `play_history_audio` as the Tauri intent and a cockpit Play action. React asks Rust for a validated asset path, converts it with `convertFileSrc`, and renders a native audio control; it does not decide file safety or read filesystem paths.
- Evidence saved at `ops/mission/evidence/2026-07-08-history-audio-playback.txt`: fmt PASS, focused playback tests PASS (2), frontend check PASS, backend tests PASS (167 lib + 2 crash recovery), clippy PASS, audit-network PASS (0 sites), ADR status PASS, state JSON PASS, npm build PASS. Honest boundary: recovered-audio re-transcription and 3-OS gate evidence remain.

## 2026-07-09T05:18Z — Codex P1 slice (model artifact install)
- Added backend-owned reviewed artifact import. `ModelEntry::install_artifact_from_path` hashes the selected source file before copy, refuses unreviewed checksums without touching the source, and reuses the existing quarantine path for bad app-data artifacts before replacement.
- Added `install_model_artifact` as the Tauri intent and registered Tauri's dialog plugin with only `dialog:allow-open`. The first-run UI opens a native file picker for required model rows, then asks Rust to install and refresh `AppSnapshot`; React does not verify or copy model bytes.
- Kept the network boundary closed. This is local file-open capability only; `scripts/audit-network.sh` still reports 0 app network call sites. Current source-tree registry TODO checksums/sources still block production readiness until reviewed model metadata lands.
- Evidence saved at `ops/mission/evidence/2026-07-08-model-artifact-install.txt`: fmt PASS, focused install tests PASS (5), frontend check PASS, backend tests PASS (172 lib + 2 crash recovery), clippy PASS, audit-network PASS (0 sites), ADR status PASS, state JSON PASS, npm build PASS, `./script/build_and_run.sh build` PASS, and `./script/build_and_run.sh --verify` PASS.

## 2026-07-09T05:29Z — Codex P1 slice (hotkey mode toggle)
- Added backend-owned hotkey mode persistence. `set_hotkey_mode` stores push-to-talk vs toggle in app-data settings.json, updates `AppSnapshot`, and applies the active runtime coordinator only while idle.
- Wired `HotkeyRuntime` startup to persisted hotkey mode and capture timing settings; toggle mode now ignores release and finalizes on the second press through the same WAL -> pipeline path.
- Added cockpit Hold/Toggle controls that send a Rust intent; React does not own hotkey semantics. Honest boundary: RightAlt/RightOption remains the only registered binding; live OS permission/conflict/rebind proof remains.
- Evidence saved at `ops/mission/evidence/2026-07-08-hotkey-mode-toggle.txt`: fmt PASS, focused hotkey-mode tests PASS (4), focused runtime tests PASS (7), frontend check PASS, backend tests PASS (178 lib + 2 crash recovery), clippy PASS, audit-network PASS (0 sites), ADR status PASS, state JSON PASS, npm build PASS, `./script/build_and_run.sh build` PASS, and `./script/build_and_run.sh --verify` PASS.

## 2026-07-09T05:39Z — Codex P1 slice (hotkey rebind)
- Added backend-owned primary binding options and persistence. `set_hotkey_binding` accepts only recommended bindings (RightAlt, F13, F14, Control+Space, Shift+F13), stores the normalized binding in app-data settings.json, and updates `AppSnapshot`.
- Changed global-shortcut handling to read the active shortcut from managed runtime state, so the app can register a new shortcut at runtime and have the existing handler observe it. Rebind refuses during active capture, registers the new shortcut before unregistering the old one, and preserves an old working binding if a candidate fails.
- Retained `HotkeyRuntime` even when the startup default shortcut registration fails, allowing the first-run one-tap rebind strip to recover without restart. Honest boundary: live OS permission/conflict proof across macOS, Windows, and Linux remains.
- Evidence saved at `ops/mission/evidence/2026-07-09-hotkey-rebind.txt` plus screenshot `ops/mission/evidence/2026-07-09-hotkey-rebind-preview.png`: fmt PASS, focused binding tests PASS (6), frontend check PASS, backend tests PASS (184 lib + 2 crash recovery), clippy PASS, audit-network PASS (0 sites), ADR status PASS, state JSON PASS, npm build PASS, browser smoke PASS, `./script/build_and_run.sh build` PASS, and `./script/build_and_run.sh --verify` PASS.

## 2026-07-09T05:43Z — Codex infra slice (GitHub remote restored)
- Repaired the private GitHub access issue without changing the remote URL. `origin` was already `https://github.com/IslandDevCrew/kaydence.git`; the active GitHub CLI account was `Navigata1`, while the stored `IslandDevCrew` login was inactive.
- Ran `gh auth switch --hostname github.com --user IslandDevCrew` and `gh auth setup-git --hostname github.com`. `gh repo view IslandDevCrew/kaydence` now resolves the repo as PRIVATE with ADMIN permission, and `git fetch origin` passes.
- Verified local main was 0 behind / 46 ahead of `origin/main` before the SOTU checkpoint. Evidence saved at `ops/mission/evidence/2026-07-09-remote-restored.txt`. Honest boundary: a fresh Actions run still must be watched after pushing the restored-access backlog.

## 2026-07-09T05:51Z — Codex P1 slice (privacy posture)
- Closed PRD P0-7's source/docs/UI posture slice: README now has explicit plain-language commitments for no telemetry, no account, no cloud screen capture, no default network egress, local-only history, and memory-only context.
- Added `scripts/check-privacy-posture.sh` and wired it into CI after `audit-network`. The gate runs the network audit, asserts the README promises remain present, and fails on app/crates source tokens for banned screen-capture implementation primitives.
- Added Screen Family 07 Privacy & Context to the cockpit, rendering privacy posture from backend-owned `AppSnapshot` plus OS-lane injection metadata. Browser smoke captured desktop/narrow screenshots with no page errors, horizontal overflow, or checked text overflow.
- Evidence saved at `ops/mission/evidence/2026-07-09-privacy-posture.txt` plus screenshots `2026-07-09-privacy-posture-desktop.png` and `2026-07-09-privacy-posture-narrow.png`. Honest boundary: future BYOK/model-download/Harbor egress still needs ADR + toggle + allowlist review before implementation.

## 2026-07-09T07:20Z — session 2 (resume: remote restored, CI green-up, Windows lane prompt)
- Repo confirmed live + PRIVATE (IslandDevCrew/kaydence, by design); the earlier 404 was a transient GitHub/credential blip — `gh auth setup-git` + retry resolves it (recorded in memory so a future session doesn't recreate the repo). All prior local commits (uinput/atspi lane) were already integrated on main.
- Caught up: a concurrent session shipped a batch of P1 features while the remote was flaky (engine asr-adapter, history export/delete/purge/play/recover, models install, hotkeys persist/rebind, privacy posture gate P1-P0-7 done). P1 now 4/9 tasks done.
- Main was RED — fixed two cross-platform CI breaks: (1) Ubuntu leg failed building alsa-sys (cpal 0.17 audio dep) for lack of libasound2-dev -> added it to ci.yml linux deps; verified full `cargo clippy --all-targets` green on the Debian VM. (2) Windows leg failed history tests with os error 32 (file-in-use): Windows can't remove_dir_all an app-data dir whose SQLite/WAL handles are open (Unix can) -> made the 11 temp-dir teardowns best-effort; history tests 19/19 + clippy green on host.
- Authored prompts/P1-P0-3-WINDOWS-INJECTION-PROMPT.md — the fable-safe build prompt for the Windows Fable box: mirror the validated Linux backend behind the shared TextInjector trait, but with Windows-native priority (UIA ValuePattern native insert PRIMARY since it works on Windows unlike AT-SPI on GNOME; SendInput Unicode fallback; UIA IsPassword for the secure-field gate; clipboard restore). Includes the live-validation + P0-G5-Windows steps the box can do that CI can't.
- Handoff: operator captures the Windows lane with that prompt. Linux lane (AT-SPI detect + uinput type) + shared core already on main; the Windows prompt owns only the Windows backend behind the same trait — no collision.

## 2026-07-09T08:20Z — session 3 (Windows injection backend — the Fable box lane)
- Repo access: the Windows box's gh CLI was authenticated as `Navigata1` (no access to the private `IslandDevCrew/kaydence`). Operator re-authorized via GitHub device flow as `IslandDevCrew`; added as a second gh account, cloned, and proceeded. (Recorded the access path in session memory so a future session verifies before assuming.)
- Built the **Windows text-injection backend** behind the existing shared `TextInjector` trait — `inject/windows.rs` (758 lines, <800). Native-primary, the inverse of the Linux ladder: UIA `ValuePattern.SetValue` (caret-aware via `TextPattern`) is the PRIMARY insert because it actually works on Windows; `SendInput` `KEYEVENTF_UNICODE` is the full-Unicode keystroke fallback; the shared `clipboard_paste()` drives the CF_UNICODETEXT snapshot→set→Ctrl+V→restore-always rung. Secure-field gate = UIA `IsPassword` (non-negotiable #8), and an unreadable password bit **fails closed** (treated as Secure). `platform_injector()` returns it on Windows; `profiles::WindowsFrontmostAppDetector` uses the blessed `GetForegroundWindow` + `QueryFullProcessImageNameW` shared helper.
- Dependency (critical decision path): `windows` 0.61 under `cfg(target_os = "windows")`, minimal features — the version **already in Cargo.lock via tauri**, no new version. Local OS input/COM/clipboard APIs, **not a network surface**: `audit-network.sh` PASS (0 sites), no allowlist entry. ADR-0013 amended (Windows amendment 2026-07-09); `inject/AGENTS.md` app-compat note added.
- **Live validation on the real desktop** (each run drives the production `inject_committed_text()` path; every claim ground-truthed): Notepad native insert → `Injected{Native}` (clean 95-char string, screenshot); WinForms password field → `Held{SecureField}`, the field's own TextChanged log stayed EMPTY (twice); classic Win32 Edit keystroke rung → exact incl. spaces+repeats; clipboard rung → text injected AND a pre-seeded `USER-CLIPBOARD-SENTINEL-42` restored intact (Pitfall P2). Honest quirk: the new WinUI/RichEdit Notepad coalesces rapid synthetic keys, so the pure keystroke FALLBACK drops repeats there — but Notepad gets native anyway; logged, not a defect.
- **P0-G5 Windows leg CLOSED**: `tauri dev` compiled + launched `kaydence.exe` and rendered the Dictation-cockpit window (title "Kaydence"), visually confirmed (screenshot). Used a prebuilt `@tauri-apps/cli` only to drive the check, then reverted it so the injection PR stays clean; `cargo-tauri` is now installed globally on this box for future runs.
- Gates on this box: fmt PASS, clippy `-D warnings` PASS, `cargo test --all` PASS (172 lib + 2 crash-recovery, incl. 11 new UIA-mapping tests), audit-network PASS, adr-status PASS, privacy-posture PASS, frontend check PASS. Evidence: `ops/mission/evidence/2026-07-09-windows-injection.txt` + 4 screenshots. HALTED for operator merge go (critical decision path: new dependency) after pushing for the 3-OS CI run.

## 2026-07-09T09:55Z — session 3 (merge go: Windows injection lane landed)
- Operator `go` received at the merge gate. PR #2 squash-merged to main as `bdbe8d6`; branch deleted. 3-OS CI was green twice pre-merge (runs 29007885003 push + 29007908105 pull_request; macos/windows/ubuntu all success); the post-merge main run is being watched to completion.
- Gate P0-G5 flipped to **passed** — all three `tauri dev` window legs now operator-observed (macOS host 2026-07-08, Debian VM 2026-07-08, Windows Fable box 2026-07-09 with screenshot evidence).
- P1-P0-3 state: Windows lane DONE + merged (native UIA primary / SendInput / clipboard-restore, secure-field refusal hard-tested). Remaining on the task: the Linux operator-in-the-loop human-focus check, unicode-beyond-ASCII on the Linux uinput keymap, and the optional portal/libei alternative.

## 2026-07-09T10:56Z — Codex P1 gate hardening (crash + short utterance)
- Synced to `ae8c00b` after PR #2; verified the latest main baseline CI run `29009328311` was green on macOS, Ubuntu, and Windows.
- Fixed a gate-quality bug: `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml crash_recovery` previously ran 0 integration tests because the test names lacked the filter string. Renamed the crash-recovery tests so the documented command now runs the real 2-test kill/recover suite.
- Added `apps/desktop/src-tauri/tests/short_utterance.rs`, a named capture/WAL suite for the implemented Pitfall-P1 contract: 0.3s push-to-talk, 1.5s toggle, <250ms discard/removal, and 0.3s/1.5s WAL recovery without truncation.
- Evidence saved at `ops/mission/evidence/2026-07-09-short-utterance-gates.txt`. Local gates passed: fmt, clippy, focused crash_recovery, focused short_utterance, full backend tests (184 lib + 2 crash + 5 short), frontend check, privacy posture, ADR status, and pnpm build. Honest boundary: P1-G2 stays pending until real ASR adapters prove golden clips have zero empty/truncated finals.

## 2026-07-09T11:27Z — Codex P1 event-sequence gate
- Added `apps/desktop/src-tauri/tests/event_sequences.rs`, the cross-module hard gate required by `tests/AGENTS.md`.
- The suite proves the typed order for happy path, focus-change hold, secure-field hold, engine fallback, and the current Full cleanup rule-floor fallback: target bind, WAL first, ASR output, cleanup, then exactly one delivery outcome.
- Refreshed mission truth: the latest pre-slice remote baseline is Actions run `29013513964` at `55e25b7`, green on macOS, Ubuntu, and Windows. This event-sequence commit still needs its own fresh CI run after push.
- Evidence saved at `ops/mission/evidence/2026-07-09-event-sequences-gate.txt`. Local gates passed: fmt, clippy, focused event_sequences, full backend tests (184 lib + 2 crash + 5 event + 5 short), frontend check, privacy posture, and ADR status. Honest boundary: this validates orchestration, not real Parakeet/Whisper recognition, golden ASR clips, Linux human-focus injection, or latency/footprint budgets.

## 2026-07-09T11:50Z — Codex P1 latency bench gate
- Confirmed the event-sequence commit is green remotely: Actions run `29015166246` at `1694fe9` completed successfully on macOS, Ubuntu, and Windows.
- Added `apps/desktop/src-tauri/src/bench.rs` plus `kaydence --bench`, a machine-readable JSON bench path that runs before Tauri startup.
- Changed `scripts/bench.sh --check` from skipped skeleton to working partial gate: it uses a packaged bench binary when present or `cargo run -- --bench` in CI, compares every measured metric to AGENTS section 5, and fails on regression. CI no longer treats bench exit 3 as a pass.
- Evidence saved at `ops/mission/evidence/2026-07-09-latency-bench-gate.txt`. Local focused gates passed: `cargo test ... bench` and `bash scripts/bench.sh --check`. Latest measured local Raw-path values: hotkey_to_capture=6ms, partial_lag=120ms, release_to_inject_cpu=7ms, light_cleanup_added=1ms. Honest boundary: P1-G3 remains pending for real Parakeet/Whisper ASR, GPU path, idle RAM/CPU, and reference-machine p95; fresh 3-OS CI is required after this workflow change.

## 2026-07-09T12:16Z — Codex P1 first-dictation completion truth
- Confirmed the latency bench-gate commit is green remotely: Actions run `29016531062` at `d531a4c` completed successfully on macOS, Ubuntu, and Windows, with the bench step enforced.
- Advanced P1-P0-8 without inventing ASR/network behavior: `first_dictation_completed` is now persisted in app-data `settings.json`, hydrated into `AppSnapshot`, and marked only after the hotkey runtime records a real `SessionEvent::Injected` outcome.
- Kept the failure boundary honest: held delivery paths, including focus-change before delivery, do not mark setup complete, so the first-run checklist cannot silently self-complete on a non-injected dictation.
- Evidence saved at `ops/mission/evidence/2026-07-09-first-dictation-completion.txt`. Local gates passed: fmt, clippy, full backend tests (188 lib + 2 crash + 5 event + 5 short), focused completion tests, frontend check, privacy posture, ADR status, and bench. Honest boundary: P1-P0-8 remains in progress for model download, permission prompts, live first-dictation journey, and <=60s proof; this slice needs fresh 3-OS CI after push.

## 2026-07-09T13:04Z — Codex P1 first-run permission readiness
- Confirmed the latest main baseline is green remotely: Actions run `29017851614` at `a89d169` completed successfully on macOS, Ubuntu, and Windows after rerunning the initially cancelled Ubuntu leg.
- Added the backend-owned first-run permission contract: `FirstRunStatus.permission_requirements` now carries platform-specific macOS, Windows, and Linux requirement rows with id, label, state, detail, and action copy. Defaults stay conservative (`needs_hardware` or `needs_review`) until real OS proof exists.
- Wired Screen Family 10 to render the runtime OS permission requirements, and Screen Family 03 to summarize the same backend contract instead of hardcoded static permission copy. React still renders `AppSnapshot`; Rust owns the canonical setup truth.
- Evidence saved at `ops/mission/evidence/2026-07-09-first-run-permission-readiness.txt`. Local gates passed: fmt, focused permission tests, frontend check, clippy, full backend tests (189 lib + 2 crash + 5 event + 5 short), privacy posture, ADR status, bench, desktop build, state JSON, and diff check. Honest boundary: this is not live permission detection; P1-P0-8 still needs real OS prompts/proof, model download/progress, first-dictation journey, <=60s proof, and fresh 3-OS CI after push.

## 2026-07-09T13:33Z — Codex P1 first-run permission action guidance
- Confirmed the post-permission-readiness main baseline is green remotely: Actions run `29020477134` at `86e50fd` completed successfully on macOS, Ubuntu, and Windows.
- Added backend-owned permission action metadata: `FirstRunPermissionRequirement.action_label` plus the `first_run_permission_action` Tauri command, which returns a structured manual step and proof requirement for the selected permission row.
- Wired Screen Family 10 permission rows to call that Rust command and render the returned guidance/proof boundary. This remains guidance, not live OS permission detection and not an OS settings deep link claim.
- Evidence saved at `ops/mission/evidence/2026-07-09-first-run-permission-actions.txt`. Local gates passed: fmt, focused permission-action tests, frontend check, clippy, full backend tests (191 lib + 2 crash + 5 event + 5 short), privacy posture, ADR status, bench, production frontend build, desktop verify/build, and Browser UI smoke. Honest boundary: P1-P0-8 still needs real OS prompts/proof, model download/progress UI, live first-dictation journey, <=60s reference proof, and fresh 3-OS CI after push.

## 2026-07-09T14:53Z — Codex P1 first-run setup timing proof
- Confirmed the post-permission-action main baseline is green remotely: Actions run `29022084029` at `699ff3b` completed successfully on macOS, Ubuntu, and Windows.
- Added backend-owned first-run setup timing truth: settings.json stores `first_run_started_at_ms` and `first_dictation_completed_at_ms`; `FirstRunStatus`/`AppSnapshot` expose `FirstRunSetupTiming` with elapsed_ms, target_ms=60000, and within_target.
- Wired runtime timing boundaries honestly: `refresh_models` starts the setup timer once when app data loads, and first-run completion only stamps completion after a real Injected delivery event.
- Added the Screen Family 10 "60-second setup proof" strip and fixed narrow desktop-window overflow found during Browser smoke by tightening grid/flex shrink rules around the sidebar, workspace, and waveform.
- Evidence saved at `ops/mission/evidence/2026-07-09-first-run-setup-timing.txt`. Local gates passed: focused timing tests, fmt, clippy, full backend tests (195 lib + 2 crash + 5 event + 5 short), frontend check, privacy posture, ADR status, bench, production frontend build, desktop verify/build, and Browser smoke at 1280x720 plus 390x844. Honest boundary: P1-P0-8 still needs real OS prompts/proof, model download/progress UI, live first-dictation journey, <=60s reference-machine proof, and fresh 3-OS CI after push.

## 2026-07-10T03:32Z — Codex P1 macOS run harness + screen-family navigation
- Repaired the canonical macOS Run/Verify path after proving three false-green/blank-window failures: missing global `cargo tauri`, broad process matching, and Tauri builds without the embedded custom protocol. The script now builds the release profile with `custom-protocol`, stages `target/release/Kaydence.app`, seals it with an ad-hoc local-development signature for `io.kaydence.app`, opens it through LaunchServices, and verifies the exact app process. `tests/build_and_run.sh` captures the launch, hangup-survival, bundle, signature-call, and custom-protocol contracts and is wired into the macOS CI leg.
- Used Computer Use against the exact signed release bundle. The full cockpit rendered at `tauri://localhost`; native F13 fallback recovered the unsupported RightAlt scancode and persisted `hotkey_primary_binding=F13`, changing the first-run hotkey row from Issue to Ready without changing any macOS security setting. Observed app RSS was 85,376 KB after 2m13s, below the P1-G3 250 MB ceiling but above the stricter <80 MB no-model product target.
- Advanced P1-G4 architecture without claiming the gate: Dictate/01, Cleanup/03, Privacy/07, and Setup/10 are now distinct left-nav views; future-phase surfaces are disabled instead of pretending to navigate. Browser red/green interaction proof passed, the 900x600 layout retained a 256px rail with no horizontal/text overflow or console errors, and the native release bundle proved Dictate -> Privacy navigation.
- Fresh local gates remained green: 225 Rust lib tests, 2 crash-recovery tests, 5 event-sequence tests, the 6-test short-utterance filter, fmt, clippy, frontend check/build, network audit, privacy posture, ADR status, and the four implemented bench metrics. P1-G2 remains pending for real-adapter golden ASR finals; P1-G4 remains pending for full board-level fidelity; P1-G3 remains pending for real ASR/GPU/idle/reference-machine evidence.
- Fresh GitHub truth: private `IslandDevCrew/kaydence` resolves with ADMIN, `git fetch origin` passes, origin/main is still `f61ab49`, and the latest run is still pre-checkout failure `29033270673`; the last verified green baseline remains `29031546518` at `ca60244`. No push attempted.
- Evidence: `ops/mission/evidence/2026-07-10-macos-run-harness-and-p1-navigation.txt` plus browser/native screenshots alongside it.

## 2026-07-10T12:45Z — session 3 (resume: verify unpushed work, confirm billing block persists)
- Resumed as primary after the Windows lane completed. Found local main 13 commits AHEAD of origin, unpushed (the prior session kept building after the Actions billing block since CI wouldn't run anyway): macos input-monitoring/accessibility proofs, cleanup default dial + secondary override hotkey, first-run permission UI/checklist, ASR runtime status, permission proofs.
- VERIFIED the tree health locally (the check CI can't do right now): macOS 225 lib tests + fmt + clippy(-D) + frontend + audit-network + privacy-posture + adr-status + bench --check all GREEN here; Linux 212 lib tests + clippy GREEN on the Debian ARM64 VM. So 2 of 3 legs are verified despite CI being down; only the Windows leg is CI-dark for the ~15 commits since the last green baseline ca60244 (all of which are macOS/first-run/ASR-readiness work, not Windows-specific).
- Pushed the 13 commits (f61ab49..c59c4fe) to preserve them + re-test billing: fresh Actions run 29093278550 FAILED in ~2-3s before checkout on all three legs — GitHub Actions is STILL billing/spending-limit blocked a day later. This is operator-only (github.com billing). CI remains the sole open verification gap.
- Assessment for the operator: (1) fix GitHub Actions billing to restore the 3-OS trust anchor; (2) biggest skipped item = P1-P0-2 REAL ASR engine (only model readiness/picker is wired — nothing transcribes yet), a critical-path dep add (whisper.cpp/ort/vad-rs + model download + Gemma license) that HALTS for operator go; (3) Linux injection human-focus live validation still owed (operator-in-the-loop at the VM).

## 2026-07-10T18:10Z — session 4 (Claude primary: verify reality, land run-harness unit)
- Resumed after Codex drove the P1 build. Re-read Plan of Attack v3; verified state.json against git reality rather than trusting claims.
- Reality confirmed: P0 DONE, P1 in_progress, Visual System v1 LOCKED (ADR-0012) with screen-family fidelity gates wired into every phase (P1-G4/P2-G3/P3-G4/P4-G-SCREENS). `main` == `origin/main`; branch `codex/p1-macos-run-harness` was 3 commits ahead (macOS Run/Verify harness + screen-family nav + heartbeat).
- Re-ran the fast gates locally (the check CI can't do while billing-dark): fmt PASS, clippy -D PASS, frontend check PASS, audit-network PASS, crash_recovery 2/2, short_utterance 6/6. All green.
- Landed the completed unit: fast-forward merged `codex/p1-macos-run-harness` into `main` (now ab5b3b9). Recorded in prLog as local merge (CI billing-blocked, no remote parity claimed).
- Re-confirmed the sole open verification gap: GitHub Actions is STILL billing/spending-limit blocked — runs 29093278550 (today) and 29033270673 (yesterday) both fail in ~3s before checkout on all three OS legs. Operator-only fix (github.com billing). Last verified-green remote baseline stays 29031546518 @ ca60244.
- HALTS surfaced for operator (unchanged, now the front of the queue): (1) fix GitHub Actions billing to restore the 3-OS trust anchor; (2) P1-P0-2 REAL ASR engine — nothing transcribes yet; the whisper.cpp/ort/vad-rs + model-download + Gemma-license dep add is a critical decision path that needs explicit go + ADR before build.

## 2026-07-10T18:23Z — session 4 cont. (P1-P0-2: first real transcription path)
- Operator go (AskUserQuestion): author ADR-0014 then build ASR. Authored ADR-0014 (real ASR engine) — whisper.cpp adapter first, feature-gated; the model-DOWNLOAD network surface kept as a SEPARATE still-Proposed sub-decision so no new egress ships tonight.
- Built engine/whisper.rs: WhisperCppEngine (whisper-rs 0.16, MIT) implementing the existing AsrEngine trait, lazy-loading a LOCAL verified ggml model and transcribing the 16kHz mono f32 WAL boundary; dictionary hints -> whisper initial prompt; per-segment partials + RawFinal; sample-rate + empty-final guards. Wired into local_asr_stack() behind feature asr-whisper (default build unchanged).
- tests/asr_golden.rs: real end-to-end golden-clip test through the production stack; SKIPS loudly unless KAYDENCE_WHISPER_MODEL is set — P1-G2 is NOT claimed passed without a real model + measured WER/latency.
- Gates GREEN on macOS: fmt; clippy -D (default AND --features asr-whisper, all-targets); build both ways (whisper.cpp compiled from source under the feature, absent by default); 225 lib + crash_recovery 2 + short_utterance 6 + event_sequences 5; audit-network (0 egress — the ASR dep added none); adr-status (ADR-0014 Accepted); frontend. Evidence: ops/mission/evidence/2026-07-10-p1-p0-2-whisper-adapter.txt.
- Merged mission/p1-p0-2-whisper-adapter -> main (b985fe2). P1-G2 note updated to reflect the adapter landing while staying honestly pending.
- Still owed: run the golden clip on a real model to close P1-G2; Parakeet/ort second adapter; the gated model-download surface (own ADR-accept); 3-OS CI once billing is fixed.

## 2026-07-10T18:35Z — session 4 cont. (P1-G2 real ASR evidence; backend label corrected 2026-07-11)
- Correction: this run used `--features asr-whisper`, which did not compile Metal. Later native backend inspection proved CPU/BLAS (`no GPU found`, `using BLAS backend`) even though Rust reported LocalGpu. The transcripts and synthetic-corpus result remain valid; the Metal and GPU-fallback claims below are superseded by `ops/mission/evidence/2026-07-10-p1-g3-whisper-metal-warmup.txt`.
- Downloaded whisper base.en (147,964,211 B, MIT, HF ggerganov/whisper.cpp) to ~/Documents/Kaydence/models (out of git). Built a synthetic golden corpus via macOS say + ffmpeg (16kHz mono).
- Ran the production local_asr_stack -> WhisperCppEngine over 4 clips (0.68s "hello there", 0.97s "send the report", 2.5s pangram, 1.8s "open the pull request and merge it"): 4/4 correct, 0 content-word WER, 249-263ms each (RTF 0.10-0.39) on CPU/BLAS. asr_golden.rs gained a dependency-free general-WAV reader (tolerates encoder LIST/INFO chunks) + audio/transcribe/RTF metrics.
- P1-G2 flipped to PASSED for the real-adapter synthetic corpus with explicit caveats: synthetic TTS is not human WER; Parakeet/ort and Windows/Linux runtime proof remain owed. Evidence: ops/mission/evidence/2026-07-10-p1-g2-golden-asr.txt. The app can genuinely turn speech into text, but this historical run did not prove Metal.

## 2026-07-10T19:52Z — session 4 cont. (billing fixed + verified; P1-G4 gap analysis)
- BILLING RESOLVED: IslandDevCrew is a personal account with a $0 Actions budget (hard-stop) that halted CI once July free minutes were spent. Fixed on the correct Chrome profile: card on file (…6046, operator-entered) + Actions budget set to $5 (hard-stop, alerts). CI diet shipped (ec3613b): pushes run Linux-only, PRs/dispatch run full 3-OS — ends the macOS ×10 and duplicate-run waste. CI proven unblocked and GREEN on Linux (run 29118854688) after CI caught a real feature-gated fmt miss in whisper.rs (fixed 6b9e18e).
- P1-G4 STARTED: viewed boards 01/03/07/10, mapped App.tsx. Verdict PENDING with a concrete gap list — invariants hold (nav order, backend-driven, local-only copy) but board composition not yet matched (01 cockpit header/status-cards/nav/footer; 03 injection+latency+test panels; 07 constitution+context-reader+audit+permissions; 10 stepper+pricing cards). Frontend is one 1914-line App.tsx to split per-view. Evidence: ops/mission/evidence/2026-07-10-p1-g4-fidelity-gap.txt.

## 2026-07-10T23:08Z - Codex P1-G4 Board 01 Dictation Cockpit
- Reconciled the v4 handoff with live v3 mission truth and followed its first explicit unit on `mission/p1-g4-cockpit`. GitHub origin/main was `3238b7b`; latest push CI run `29120511458` was green under the new Linux-push/full-PR matrix policy.
- Rebuilt Dictate to the locked Board 01 composition: recording strip, five backend-derived status cards, transcript/history, in-view cleanup and output controls, five-action nav, and two-line status footer. Browser-only fixtures exercise the ready/recording state; the signed native app keeps unavailable ASR/target/WAL/microphone evidence Blocked or Pending.
- Extracted shared cockpit primitives to `components/CockpitChrome.tsx` and Board 01 to `views/DictateView.tsx` + `DictateView.css`, each under 800 lines. The remaining App/styles monolith will shrink board by board rather than through one unreviewable rewrite.
- Product Design QA iterated on real evidence: fixed 97px viewport overflow, then caught and repaired WebKit's failure to render external SVG sprite fragments, then tightened waveform density, transcript depth, target copy, meter, and auto-paste toggle. Combined source comparison and final `design-qa.md` are scoped PASS for Board 01.
- Browser proof passed at 540x800 and 900x600 with exact viewport-sized scroll bounds, working record preview, cleanup dial, OS lanes, and nav round trip; a fresh final tab had zero warnings/errors. Computer Use proved the signed native bundle at `tauri://localhost`, F13 registration, backend Raw/Light persistence, and macOS/Windows/Linux lane copy/accent changes.
- Fresh local gates: fmt, clippy -D, 225 lib + 12 integration tests, frontend check/build, build/run harness, audit-network (0), privacy, ADR, and four measured bench fields all green. P1-G3 remains partial for real GPU/idle/reference evidence. P1-G4 remains pending for Boards 03/07/10, real Windows/Linux WebView captures, and final human sign-off.
- Evidence: `ops/mission/evidence/2026-07-10-p1-g4-board01.txt` plus Browser/native/comparison PNGs.

## 2026-07-11T00:22Z - Codex P1-G4 Board 03 Cleanup & Injection
- Confirmed Board 01 merged through PR #3 at `ac90ec6`; full PR matrix `29130796977` and post-merge Linux run `29131422736` are green. Removed the resolved billing incident from active blockers while preserving the cost-controlled Linux-push/full-PR workflow.
- Rebuilt Cleanup to the locked Board 03 composition: compact source-order rail, persisted Raw/Light/Full dial, seven read-only rule states, three before/after examples, per-app destination evidence, OS-specific primary/fallback injection paths, health, real latency budgets, four gates, and reset/save actions. Future rule/profile editors remain disabled because no backend command exists.
- Final review caught and repaired a behavioral regression before merge: the replaced Cleanup page had been the only always-visible hotkey mode/rebind surface. Hold/Toggle and all five binding choices now live in Setup, with working Browser fixture updates and backend persistence retained.
- Browser QA passed at 540x800 and 900x600 with exact document and panel scroll bounds, working Raw/Full/Reset/Save controls, macOS/Windows/Linux copy and accents, permission-navigation round trip, and zero fresh console errors. The RED baseline was 885x1074 at a 900x600 viewport.
- Computer Use proved the exact signed native bundle at `tauri://localhost`: conservative backend permissions/delivery/p95 state, persisted Raw/Full/Light transitions, three lane capability sets, and the real Screen Family 10 permission path. A WebKit-only clipped Round Trip row was fixed with a short-window density contract; the final native capture has all four gates and no internal scrollbar.
- Fresh local gates are green: fmt, clippy `-D warnings`, 225 lib + 12 integration tests, frontend check/build, build/run harness, strict signature, audit-network (0), privacy, ADR, and four measured bench fields. P1-G3 remains partial for real GPU/prediction/idle/reference evidence. P1-G4 advances to 2/4 but remains pending for Boards 07/10, real Windows/Linux WebView captures, and human sign-off.
- Evidence: `ops/mission/evidence/2026-07-10-p1-g4-board03.txt`, Browser/native/comparison images, and the appended Board 03 section in `design-qa.md`.

## 2026-07-11T01:04Z - Codex P1-G4 Board 07 Privacy & Context
- Closed the prior Board 03 delivery loop first: PR #4 squash-merged at `0513d15`; full macOS/Windows/Ubuntu PR matrix `29132985360` and post-merge Linux run `29133457419` are green.
- Rebuilt Privacy to the locked Board 07 hierarchy: compact source-order rail, four-part Constitution, six-row Context Reader, OS-aware permissions, metadata-only local audit, and status footer. The Constitution dialog, Setup handoff, History navigation, and three lane controls are functional.
- Kept the product contract ahead of the mock: the view discloses 30-day local persistence, leaves context and OCR off for P3 opt-in, marks screen capture Not requested, uses Rust-owned permission states on the host lane, labels non-host lanes as references, and never renders transcript text in the audit list.
- Browser QA passed at 540x800 and 900x600 with exact document and panel bounds, all lane accents/vocabularies, working navigation/dialog controls, and zero warnings/errors. The combined comparison records the intentional truth differences from the illustrative source.
- Computer Use proved the exact signed macOS bundle at `tauri://localhost`: conservative backend permission state, Constitution modal, all three lane vocabularies, real Screen Family 10 Setup handoff, and restored macOS Privacy state. Strict codesign verification passed.
- Fresh local gates are green: fmt, clippy `-D warnings`, 225 lib + 12 integration tests, frontend check/production build, build/run contract, network audit (0), privacy posture, ADR status, and four measured bench fields. P1-G4 advances to 3/4 but remains pending for Board 10, real Windows/Linux WebView captures, and final human sign-off.
- Evidence: `ops/mission/evidence/2026-07-11-p1-g4-board07.txt`, Browser/native/comparison images, and the appended Board 07 section in `design-qa.md`.

## 2026-07-11T01:58Z - Codex P1-G4 Board 10 First Run & License
- Closed the prior Board 07 delivery loop first: PR #5 squash-merged at `c3412cb`; full macOS/Windows/Ubuntu PR matrix `29134025617` and post-merge Linux run `29134596004` are green.
- Rebuilt Setup to the locked Board 10 hierarchy: compact nine-step rail, progress line, backend-owned next-step strip, six setup controls, four license/service choices, accepted pricing clarification, and a stable action footer. Deep model, permission, timing, and proof detail remains available through a keyboard-closeable Evidence dialog.
- Kept current capability truth visible: Whisper-Ahead is disabled/P3; only Free is selectable; desktop capability is one-time while Harbor is optional and self-hostable; model, permission, hotkey, timing, and first-dictation state come from Rust in the native app. The dictation/complete primary action now reaches the cockpit.
- Browser QA passed at 540x800 and 900x600 with exact bounds, working selectors/toggles/dialog/navigation, three accents, explicit Windows/Linux permission vocabulary, disabled off-host runtime actions, and zero warnings/errors. Combined source comparison records the intentional truth differences from the illustrative board.
- Computer Use proved the signed macOS bundle, blocked model-metadata boundary, permission guidance, local proof export, and all three lane contracts. Native review caught and repaired one real defect before delivery: Windows initially inherited macOS permission labels. The final build uses explicit Windows `UI Automation / Keyboard fallback` and Linux `AT-SPI / uinput` references and restores the macOS host state.
- Fresh local gates are green: fmt, clippy `-D warnings`, 225 lib + 12 integration tests, frontend check/production build, build/run contract, strict signature, network audit (0), privacy posture, ADR status, and four measured bench fields. P1-G3 remains partial for real GPU/prediction/idle/reference evidence.
- All four P1 boards now have scoped design-QA passes. P1-G4 remains pending for real Windows/Linux WebView captures and final human sign-off; P1-P0-8 remains open for reviewed model metadata/download handling, remaining permission proof, live first dictation, and <=60-second reference timing.
- Evidence: `ops/mission/evidence/2026-07-11-p1-g4-board10.txt`, Browser/native/comparison images, and the appended Board 10 section in `design-qa.md`.

## 2026-07-11T02:38Z - Codex P1-G4 native Linux WebView proof
- Closed the Board 10 delivery loop: PR #6 squash-merged at `1505fbb`; full macOS/Windows/Ubuntu PR matrix `29135724151` and post-merge Linux run `29136083090` are green.
- Reconnected to the existing UTM reference VM at `192.168.64.4`, verified Debian 12 ARM64 with an active GNOME/Mutter Wayland session, and synced current main into the VM. Sha256 parity matched for `App.tsx`, `FirstRunView.tsx`, and both first-run CSS files.
- Installed the two current frontend packages missing from the VM cache, then launched the real Tauri dev app inside the Wayland session. Vite started on localhost and the Rust app compiled from the warm ARM64 cache in 6.03 seconds. GTK fell back from unavailable GL to software rendering without a runtime failure.
- Computer Use navigated the native guest window through keyboard-reachable controls and captured all four P1 families. Board 01 showed Linux cockpit/output truth; Board 03 showed the AT-SPI/uinput injection ladder and gates; Board 07 showed the privacy Constitution and backend Linux permissions; Board 10 showed the Emerald setup/license lane and accepted pricing copy.
- Opened Board 10's native Permissions dialog and proved the backend-owned `Microphone`, `AT-SPI accessibility bus`, and `uinput keyboard path` rows with conservative Hardware Proof/Review states.
- Saved clean 900x600 crops, uncropped UTM/GNOME provenance frames, and a four-board contact sheet. The Linux WebView requirement is now complete. P1-G4 remains pending only for real Windows WebView captures and final human sign-off.
- This did not close the separate Linux human-focus injection test; normal-field delivery and password-field refusal remain operator-in-the-loop P1-P0-3 proof.
- Evidence: `ops/mission/evidence/2026-07-11-p1-g4-native-linux.txt` and `ops/mission/evidence/2026-07-11-p1-g4-native-linux-contact-sheet.png`.

## 2026-07-11T05:03Z - Codex Windows diagnostics + truthful Metal warmup
- Closed the Linux proof delivery loop first: PR #7 squash-merged at `7e7a817`; full PR matrix `29136824031` and post-merge Linux run `29137177218` were green.
- Built and merged the hosted Windows WebView proof harness in PR #8 (`0d30883`) after full 3-OS run `29137597560`. It launches the release app, navigates with Windows UI Automation, captures exact client/titlebar images, preserves UIA/source/host manifests, checks pixels, and fails closed.
- Hosted run `29137950481` captured clean Boards 01/03/07/10 on Windows Server 2025, but stopped before the permissions modal because the harness expected shorthand `UI Automation` while production exposes `UI Automation focus access`. The four images are preserved as diagnostics, not passing proof. PR #9 fixed the exact labels and passed full run `29138662377` before merge at `80c9374`.
- A second hosted proof VM never exposed the initial WebView UIA subtree. Draft PR #10 adds forced renderer accessibility, three bounded startup attempts, process restart, and unchecked failure artifacts. GitHub refused to start run `29139988632` and explicitly reported failed account payments or an insufficient spending limit. PR #10 remains open and unmerged.
- Created a same-viewport combined comparison for all four Windows captures against the already source-reviewed 900x600 Browser fixtures. Composition, density, cobalt lane accent, icons, footer/navigation bounds, and text fit remain consistent with no visible overlap or crop. This is a diagnostic design pass only; Windows permissions proof and final human signoff remain open.
- Audited P1-G3 and found the prior Metal claim was false: `asr-whisper` compiled no GPU backend and whisper.cpp had used CPU/BLAS while Rust reported LocalGpu. Added explicit `asr-whisper-metal`, corrected no-acceleration routing to LocalCpu, and introduced pre-dictation warmup for the first viable lane with fallback-on-init-failure and no double model load.
- Runtime readiness now stays false until warmup succeeds; warmup failure marks ASR blocked and preserves the previous processor. Real Apple M5 Pro logs prove the Metal backend. The 681 ms clip passed at 59 ms warm Metal inference after 529 ms warmup and 208 ms CPU inference after 487 ms warmup; both tests enforce their lane budgets. A no-Metal GPU request correctly warmed and ran LocalCpu.
- Review then closed two hot-path/readiness defects: a lane whose warmup failed is now recorded but skipped during transcription instead of being retried, and backend/UI first-run readiness now requires `asr_runtime.adapter_ready` rather than treating a verified model artifact as a usable engine. Setup exposes a dedicated Prepare/Repair ASR runtime step and Retry runtime action before permissions.
- Full local feature gates are green: 232 Rust unit tests plus crash/event/short-utterance integrations, true Metal and CPU golden runs, no-Metal lane regression, fmt, and clippy `-D warnings`. P1-G3 remains pending for full release-to-inject, ASR-resident RAM/CPU, reference p95, Windows/Linux runtime proof, and the no-model process-group gap (30 MB main process, about 86 MB including WebKit vs <80 MB target).
- Corrected the original evidence, ADR-0014, engine contract, mission state, design QA, journal, and generated State of the Union so no release artifact repeats the false GPU claim.

## 2026-07-11T05:24Z - Codex canonical ASR packaging + first-run scope
- Closed the release-feature disconnect: `script/build_and_run.sh` now compiles macOS with `custom-protocol,asr-whisper-metal` and Windows/Linux with `custom-protocol,asr-whisper`; the hosted Windows WebView proof compiles the same CPU adapter. The build/run contract was captured red before the production edit and passes afterward. A real optimized macOS build of the shipped feature combination also passes.
- Tightened first-run model truth. Readiness now requires the selected ASR artifact plus only support models explicitly marked `first_run_required`, rather than every recommended ASR and the currently unused Silero model. The runtime still requires successful adapter warmup before it can report Ready or advance setup to permissions.
- Reverified the real 681 ms golden clip twice: Apple M5 Pro Metal backend, exact `Hello there`, 597-649 ms startup warmup and 55-67 ms warm inference; explicit CPU fallback, 511 ms warmup and 208 ms inference; no-Metal build requested as GPU, honestly reported LocalCpu and completed in 217 ms.
- Senior review closed a setup recovery defect: runtime warmup failure already marked backend state Blocked but rejected the response, leaving React on a stale snapshot. Warmup failure now returns the Blocked snapshot while active-capture/runtime-lock coordination failures still reject; the test was captured red then green.
- PR CI now runs strict Clippy and all-target tests for the actual shipped adapter on every OS (`asr-whisper-metal` on macOS, `asr-whisper` on Windows/Linux) in addition to the default gates. A future green matrix will therefore validate the binary feature contract instead of only the developer build.
- Fresh local verification is green: 232 default Rust tests and integrations, 234 Metal-feature Rust tests and integrations, strict Clippy in both modes, frontend type/lint/build, build/run contract, optimized release build, privacy/network posture, ADR status, raw benchmark, and the three real-model lane proofs. `bench-prediction.sh` remains a later P3 dependency and reports `llama-cli` absent.
- P1-G3 remains pending for full release-to-inject timing, ASR-resident RAM/idle CPU, the no-model full-process-group overage, Windows/Linux runtime proof, and reference-machine p95. P1-G4 remains pending for a passing Windows permissions capture and final human signoff. GitHub billing still prevents a fresh 3-OS matrix, so this branch is not merge-ready.
- Checkpointed the work as commit `3ccf2d9` and draft PR #11. Its new Actions run `29141559030` failed at setup in two seconds with zero runner/steps; annotation: recent account payments failed or the spending limit must be increased. No code or test failure occurred. PR #11 remains open and intentionally unmerged.

## 2026-07-11T06:14Z - Codex Linux ARM64 Whisper runtime + portable release hardening
- Ran the exact `eaf82f6` source archive, byte-matched base.en model, and byte-matched 681 ms golden clip in the active Debian 12 ARM64 UTM VM while leaving the native Linux Kaydence app open.
- Found a real upstream build-host defect: whisper.cpp's native ARM detection generated an FP16 feature combination GCC 12 could not compile. `GGML_NATIVE=OFF` compiled cleanly; its generic ARMv8 path transcribed exactly but took 1500 ms, so that attempt remained a diagnostic failure against the 1200 ms CPU budget.
- Rebuilt with native autodetection still disabled and only the VM-advertised `armv8.2-a+fp16+dotprod` capabilities. The production stack returned exact `Hello there`, warmed in 65 ms, inferred in 999 ms, and truthfully reported `LocalCpu` after a GPU request with no Linux accelerator feature. The hard CPU assertion passed.
- Hardened the release boundary test-first: `tests/build_and_run.sh` failed before the production edit, then passed after `script/build_and_run.sh` defaulted `GGML_NATIVE=OFF`. Review caught that upstream does not rerun on this env change and that a profile-less clean can leave release artifacts intact; the final build-config stamp invalidates only the matching debug/release Whisper profile. The contract proves first-build clean, unchanged reuse, architecture-change clean, and release-profile selection. PR CI uses the same portable default.
- Cleared the real macOS release Whisper profile and rebuilt `custom-protocol,asr-whisper-metal` in 1m26s. CMake proves `GGML_NATIVE:BOOL=OFF` and `GGML_METAL:BOOL=ON`; the real clip remained exact and green at 653 ms warmup plus 59 ms Metal inference.
- Linux ASR runtime proof is now closed for the tested ARM64 capability build. P1-G3 remains pending for complete release-to-inject timing, ASR-resident RAM/idle CPU, the 86 MB no-model full-process-group gap, reference p95, and Windows runtime proof.
- Final local verification is green: 232 default Rust tests plus integrations, 234 Metal-feature tests plus integrations, strict Clippy in both modes, real portable Metal golden clip, optimized portable release build, frontend type/lint/build, build/cache contract, fmt, workflow YAML, mission JSON/render, privacy/network, ADR, and the measured Raw benchmark fields.
- Pushed code/evidence commit `72f6013` and refreshed draft PR #11's body. Actions run `29142969657` failed in three seconds before checkout: setup had zero steps, the matrix job was skipped, and GitHub again reported failed payments or an exhausted spending limit. This is not a code/test failure; PR #11 remains intentionally unmerged.
- Evidence: `ops/mission/evidence/2026-07-11-p1-g3-linux-whisper-runtime.txt` plus the updated cross-platform P1-G3 evidence record.

## 2026-07-11T06:42Z - Codex Linux human-focus injection closure
- Used the isolated `eaf82f6` integrated `atspi-selftest` in the already-running Debian 12 GNOME/Wayland VM. The harness ran as the logged-in `debian` user on the real AT-SPI session bus with the existing uinput permission.
- Normal-field proof passed without scripted focus: Alt+F2 opened GNOME's real command entry, AT-SPI classified `role=Text editable=true`, and the uinput backend visibly typed `[Kaydence]` into the focused field.
- Secure-field proof passed on a real Zenity password entry: AT-SPI classified `role=PasswordText editable=true`, Kaydence mapped it to `Secure`, refused twice across focus events, and the field remained empty. No password was entered or submitted; the dialog was cancelled.
- Preserved exact logs plus full 1152x768 UTM provenance frames. Removed the Linux human-focus item from active blockers and updated ADR-0013, the Wayland spike, the injection contract, P1-P0-3 state, risk, and resume truth.
- P1-P0-3 remains in progress only for Linux Unicode beyond ASCII, optional portal/libei where uinput permission is unavailable, and the disclosed opaque-client policy boundary; the operator normal/password focus check itself is complete.
- Opened stacked draft PR #12 from `codex/p1-p0-3-linux-human-focus`, based on PR #11's branch so its diff stays limited to this proof. Retarget it to `main` after PR #11 lands; the mandatory matrix remains billing-blocked.
- Evidence: `ops/mission/evidence/2026-07-11-linux-human-focus-injection.txt`, `2026-07-11-linux-human-focus-normal.jpg`, and `2026-07-11-linux-human-focus-secure.jpg`.
